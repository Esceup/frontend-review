export default {
  id: "css",
  title: "CSS",
  accent: "#43d2ff",
  topics: [
    {
      id: "cascade",
      title: "Каскад и наследование",
      questions: [
        {
          id: "css-01",
          hot: true,
          q: "Что такое каскадность и специфичность в CSS?",
          a: "Каскадность (cascading) — это процесс объединения стилей из разных источников и разрешения конфликтов на основе важности (!important), происхождения (user agent, user, author) и специфичности. Специфичность — это вес селектора: inline-стили (1000) > ID (100) > классы/псевдоклассы/атрибуты (10) > теги/псевдоэлементы (1). Чем выше специфичность, тем приоритетнее правило. При равной специфичности побеждает последнее объявленное.",
        },
        {
          id: "css-02",
          q: "Какие есть способы подключения CSS к HTML?",
          a: "1) **Внешний**: `<link rel=\"stylesheet\" href=\"style.css\">` — рекомендуется для разделения стилей и структуры, кэшируется браузером. 2) **Внутренний**: `<style> ... </style>` в head — для специфичных стилей конкретной страницы. 3) **Инлайн**: `style=\"color:red;\"` у тега — наивысший приоритет (специфичность 1000), но плохо поддерживается и не переиспользуется. Альтернатива внешнему — `@import url(\"style.css\")` внутри `<style>`, но он медленнее и блокирует параллельную загрузку.",
        },
        {
          id: "css-22",
          q: "Как работает наследование в CSS?",
          a: "Наследование — механизм, при котором дочерние элементы получают значения некоторых свойств от родителей. Наследуются: типографика (font-size, color, line-height, text-align), списки (list-style). Не наследуются: размеры (width, height), отступы (margin, padding), границы (border), позиционирование. Можно принудительно наследовать: `margin: inherit;` или сбросить: `all: initial;`.",
        },
      ],
    },
    {
      id: "selectors",
      title: "Селекторы и псевдоклассы",
      questions: [
        {
          id: "css-03",
          q: "Что такое CSS-селекторы и какие основные типы вы знаете?",
          a: "Селекторы — основа CSS. Примеры: `div` (все div), `.header` (класс), `#main` (ID), `[type=\"text\"]` (атрибут), `:first-child` (псевдокласс), `::after` (псевдоэлемент). Комбинаторы: `div p` (потомок), `div > p` (прямой потомок), `div + p` (сосед), `div ~ p` (все следующие соседи). Специфичность растёт с количеством селекторов в цепочке.",
        },
        {
          id: "css-08",
          q: "Что такое псевдоклассы и псевдоэлементы? Приведите примеры.",
          a: "Псевдоклассы добавляются к селектору с одним двоеточием, например, `a:hover` или `li:first-child`. Они определяют состояние (наведение, фокус, позиция). Псевдоэлементы — с двумя двоеточиями (`::before`, `::after`, `::first-line`), создают виртуальные элементы или стилизуют части элемента. Например, `div::before` вставляет контент до содержимого div.",
        },
        {
          id: "css-33",
          q: "Что делают псевдоклассы `:is()`, `:where()` и `:has()`?",
          a: "**:is()** — принимает список селекторов, упрощает запись: `:is(h1, h2, h3) { color: red; }` вместо `h1, h2, h3 { ... }`. Специфичность равна самому специфичному селектору в списке. **:where()** — то же самое, но специфичность всегда 0, что полезно для сброса стилей. **:has()** — «родительский селектор», выбирает элемент, содержащий определённых потомков: `div:has(> p)` — div с прямым потомком p. Один из самых ожидаемых псевдоклассов, теперь поддерживается во всех современных браузерах.",
        },
        {
          id: "css-35",
          q: "Что такое CSS Nesting (вложенность)?",
          a: "Пример: `.parent { color: red; .child { color: blue; &:hover { color: green; } } }`. Символ `&` ссылается на родительский селектор. Поддерживаются псевдоклассы: `.button { &:hover { ... } }`, медиа-запросы: `.container { @media (min-width: 768px) { ... } }`. Упрощает чтение и поддержку кода, уменьшает дублирование. Поддерживается во всех современных браузерах с 2023 года.",
        },
      ],
    },
    {
      id: "boxmodel",
      title: "Блочная модель и display",
      questions: [
        {
          id: "css-21",
          hot: true,
          q: "Что такое CSS Box Model и из чего он состоит?",
          a: "Box Model состоит из четырёх областей: **content** (содержимое), **padding** (внутренний отступ), **border** (граница), **margin** (внешний отступ). По умолчанию (content-box) width/height задаются только для content, padding и border добавляются сверху. С box-sizing: border-box размеры включают padding и border. Margin может схлопываться (collapsing) у соседних вертикальных элементов.",
        },
        {
          id: "css-04",
          hot: true,
          q: "В чём разница между `display: block`, `inline`, `inline-block`?",
          a: "**block**: элемент занимает всю доступную ширину, начинается с новой строки, можно задавать width/height/margin/padding. **inline**: не переносит строку, ширина определяется содержимым, нельзя задать высоту/ширину, вертикальные margin не работают (padding визуально работает, но не сдвигает соседние строки). **inline-block**: не переносит строку, но можно задавать размеры, отступы и рамки — гибрид обоих подходов.",
        },
        {
          id: "css-11",
          hot: true,
          q: "Как работает `box-sizing`?",
          a: "По умолчанию **box-sizing: content-box** — ширина задаётся только для содержимого, padding и border добавляются сверху (width: 100px + padding: 10px = 120px). **border-box** — ширина включает padding и border (width: 100px + padding: 10px = 100px, содержимое сжимается). Часто применяют глобально: `* { box-sizing: border-box; }` для упрощения расчётов макета.",
        },
        {
          id: "css-30",
          q: "В чём разница между `outline` и `border`?",
          a: "**border** — часть box model, влияет на размеры элемента (с box-sizing: content-box), может быть скруглена через border-radius, поддерживает разные стили для каждой стороны. **outline** — рисуется поверх border, не влияет на размеры и поток, не может быть скруглена (в некоторых браузерах есть outline-offset), обычно используется для фокуса (focus ring). Outline не наследуется и не может быть задана для отдельных сторон.",
        },
        {
          id: "css-23",
          q: "Что делает свойство `overflow`?",
          a: "Значения: **visible** (по умолчанию, содержимое выходит за границы), **hidden** (обрезается, скролл недоступен), **scroll** (всегда показывается скролл, даже если не нужен), **auto** (скролл появляется только при необходимости). Можно задавать отдельно: `overflow-x`, `overflow-y`. Часто используется для создания кастомных скроллбаров, обрезки текста (text-overflow: ellipsis).",
        },
      ],
    },
    {
      id: "position",
      title: "Позиционирование и слои",
      questions: [
        {
          id: "css-07",
          hot: true,
          q: "Как работает позиционирование (relative, absolute, fixed, sticky)?",
          a: "**static** — по умолчанию, элемент в обычном потоке. **relative**: элемент смещается относительно своего нормального положения, но занимает место в потоке. **absolute**: полностью удаляется из потока, позиционируется относительно ближайшего positioned предка (не static). **fixed**: относительно viewport, не двигается при скролле. **sticky**: ведёт себя как relative до достижения порога (top/bottom), затем фиксируется как fixed.",
        },
        {
          id: "css-12",
          hot: true,
          q: "Что такое `z-index` и когда он работает?",
          a: "Элементы с большим z-index находятся выше. z-index работает **только** на элементах с position (кроме static). Также создаётся новый контекст наложения (stacking context) для дочерних элементов. По умолчанию элементы в DOM перекрывают друг друга в порядке следования (последний выше). Отрицательные значения z-index позволяют поместить элемент под родительский.",
        },
        {
          id: "css-24",
          q: "Что такое `float` и `clear` и почему они устарели?",
          a: "**float** (left/right) изначально предназначался для обтекания изображений текстом, но стал использоваться для создания колоночных макетов. Требовал хаков (clearfix) для очистки. **clear** (left/right/both) запрещал обтекание с указанной стороны. С появлением flexbox и grid float устарел для layouts, но всё ещё используется для обтекания текста вокруг изображений.",
        },
      ],
    },
    {
      id: "flex",
      title: "Flexbox",
      questions: [
        {
          id: "css-05",
          hot: true,
          q: "Что такое flexbox и для чего он используется?",
          a: "Flexbox позволяет легко выравнивать элементы внутри контейнера по основной и поперечной осям. Основные свойства контейнера: `display: flex`, `flex-direction` (row/column), `justify-content` (выравнивание по главной оси), `align-items` (по поперечной), `flex-wrap`. Свойства дочерних элементов: `flex-grow`, `flex-shrink`, `flex-basis`. Идеально подходит для компонентов интерфейса, навигации, карточек.",
        },
        {
          id: "css-36",
          q: "Что делает свойство `gap` в flexbox и grid?",
          a: "Пример: `display: flex; gap: 20px;` или `display: grid; gap: 10px 20px;` (rows columns). В отличие от margin, gap применяется только между элементами, не добавляя отступы по краям контейнера. Можно задавать отдельно: `row-gap`, `column-gap`. Работает только в flex и grid контейнерах, не в обычном потоке. Упрощает создание равномерных интервалов без хаков с отрицательными margin.",
        },
      ],
    },
    {
      id: "grid",
      title: "Grid и центрирование",
      questions: [
        {
          id: "css-06",
          hot: true,
          q: "Что такое CSS Grid и для чего он используется?",
          a: "CSS Grid Layout даёт возможность управлять расположением элементов в двух измерениях одновременно. Определяется сетка через `grid-template-columns`, `grid-template-rows`, размещение элементов через `grid-column`, `grid-row` или `grid-area`. Поддерживает именованные области, авто-размещение, `minmax()`, `repeat()`. Позволяет создавать сложные макеты (dashboard, галереи) без дополнительных обёрток.",
        },
        {
          id: "css-13",
          hot: true,
          q: "Как центрировать элемент по вертикали и горизонтали?",
          a: "Современные способы: 1) **flexbox**: родитель `display: flex; justify-content: center; align-items: center;`. 2) **grid**: `display: grid; place-items: center;`. 3) **absolute**: `top: 50%; left: 50%; transform: translate(-50%, -50%);`. 4) для строчных элементов: `text-align: center` и `line-height` равный высоте. Старые методы (table-cell, margin: 0 auto с фиксированной шириной) устарели.",
        },
      ],
    },
    {
      id: "units",
      title: "Единицы и функции",
      questions: [
        {
          id: "css-15",
          hot: true,
          q: "Разница между `em` и `rem`?",
          a: "**em** — относительная единица, зависящая от размера шрифта родительского элемента. Может вызывать каскадные эффекты (вложенные элементы увеличиваются). **rem** (root em) — относительно размера шрифта корневого элемента (`html`, обычно 16px по умолчанию). Использование rem упрощает масштабирование всего интерфейса, меняя только корневой размер. Рекомендуется использовать rem для глобальных размеров, em — для локальных (например, padding внутри компонента).",
        },
        {
          id: "css-16",
          q: "Что такое `calc()` в CSS?",
          a: "Пример: `width: calc(50% - 10px);`. Поддерживает +, -, *, /. Позволяет смешивать проценты, пиксели, em и т.д. Полезна для адаптивных макетов, где нужно учитывать отступы. Можно вкладывать: `calc(100% - calc(20px * 2))`. Работает в большинстве свойств, принимающих числовые значения (width, height, margin, padding, font-size и др.).",
        },
        {
          id: "css-31",
          q: "Какие относительные единицы измерения есть в CSS (vw, vh, vmin, vmax, ch)?",
          a: "**vw** (viewport width) — 1% от ширины viewport. **vh** (viewport height) — 1% от высоты. **vmin** — меньшее из vw и vh. **vmax** — большее из vw и vh. **ch** — ширина символа «0» в текущем шрифте (полезно для ограничения ширины текста). Также есть **%** (относительно родителя), **em** (относительно шрифта родителя), **rem** (относительно корневого шрифта). Эти единицы помогают создавать адаптивные макеты без медиа-запросов.",
        },
        {
          id: "css-32",
          q: "Что делают функции `min()`, `max()` и `clamp()`?",
          a: "Примеры: `width: min(50%, 300px);` — возьмёт меньшее из 50% и 300px. `height: max(100px, 50vh);` — возьмёт большее. `font-size: clamp(16px, 2vw, 24px);` — значение между 16px и 24px, в идеале 2vw. Эти функции позволяют создавать адаптивные значения без медиа-запросов. Можно комбинировать с calc(): `width: calc(min(50%, 300px) - 20px);`.",
        },
      ],
    },
    {
      id: "vars",
      title: "Переменные и @-правила",
      questions: [
        {
          id: "css-10",
          q: "Что такое CSS-переменные (custom properties)?",
          a: "Пример: `:root { --main-color: #3498db; }` затем `color: var(--main-color);`. Переменные наследуются, могут быть изменены через медиа-запросы и JavaScript (`element.style.setProperty(\"--main-color\", \"red\")`), что облегчает создание динамических тем. Можно задавать fallback: `var(--color, blue)`. В отличие от препроцессоров, работают в рантайме.",
        },
        {
          id: "css-34",
          q: "Что такое @-правила: `@media`, `@supports`, `@layer`?",
          a: "**@media** — применяет стили при определённых условиях (ширина экрана, ориентация): `@media (max-width: 768px) { ... }`. **@supports** — проверяет поддержку свойства: `@supports (display: grid) { ... }` с fallback через `@supports not`. **@layer** — определяет каскадные слои для управления приоритетом стилей: `@layer base, components, utilities;`. Стили в более позднем слое имеют больший приоритет, независимо от специфичности.",
        },
      ],
    },
    {
      id: "adaptive",
      title: "Адаптивность",
      questions: [
        {
          id: "css-09",
          hot: true,
          q: "Как сделать адаптивный дизайн?",
          a: "Основные инструменты: **media queries** (изменение стилей в зависимости от ширины экрана, ориентации), **относительные единицы** (%, vw, vh, rem, em) вместо фиксированных пикселей, **flexbox и grid** для гибкого расположения, **picture/source** для адаптивных изображений, **clamp()** для плавного масштабирования. Mobile-first подход: сначала стили для мобильных, затем медиа-запросы для больших экранов.",
        },
        {
          id: "css-37",
          q: "Что делает свойство `aspect-ratio`?",
          a: "Пример: `aspect-ratio: 16 / 9;` или `aspect-ratio: 1;` (квадрат). Если задана width, height вычисляется автоматически, и наоборот. Полезно для видео, изображений, карточек с фиксированными пропорциями. Можно комбинировать с min/max-width/height. Заменяет старый хак с padding-top в процентах (padding-top: 56.25% для 16:9). Поддерживается во всех современных браузерах с 2021 года.",
        },
        {
          id: "css-38",
          q: "Что делают свойства `scroll-snap` и `scroll-behavior`?",
          a: "**scroll-snap-type** (на контейнере): `x mandatory` или `y proximity` — определяет ось и строгость привязки. **scroll-snap-align** (на дочерних элементах): `start`, `center`, `end` — точка привязки. Создаёт эффект карусели или слайдера без JavaScript. **scroll-behavior**: `smooth` — плавная прокрутка при клике на якорные ссылки или программном скролле. `auto` — мгновенная прокрутка (по умолчанию). Можно задавать для html: `html { scroll-behavior: smooth; }`.",
        },
      ],
    },
    {
      id: "decoration",
      title: "Оформление: фоны и эффекты",
      questions: [
        {
          id: "css-17",
          q: "Как работает `object-fit` для изображений?",
          a: "Свойство object-fit работает для заменяемых элементов (img, video, iframe). Значения: **fill** (растянуть, может исказить), **contain** (вписать без обрезки, могут появиться пустые области), **cover** (заполнить контейнер, обрезая края), **none** (оригинальный размер), **scale-down** (выбирает наименьший из none и contain). Часто используется с `object-position` для управления точкой фокуса.",
        },
        {
          id: "css-19",
          q: "Как задать градиент в CSS?",
          a: "Пример: `background: linear-gradient(to right, red, blue);` или `radial-gradient(circle, yellow, green);`. Можно задавать несколько цветов и направления (45deg, to bottom right). Также есть `conic-gradient()` для конических градиентов. Градиенты генерируются CSS без загрузки изображений, что улучшает производительность. Можно комбинировать с изображениями: `background: linear-gradient(...), url(...)`.",
        },
        {
          id: "css-28",
          q: "Как работает сокращённая запись `background`?",
          a: "Пример: `background: #f0f0f0 url(\"image.png\") no-repeat center/cover fixed;`. Порядок: color, image, repeat, attachment, position/size. Можно указывать не все свойства, остальные получат значения по умолчанию. Поддерживает несколько фонов через запятую: `background: url(top.png) top, url(bottom.png) bottom;`. **background-size** можно задавать через слэш после position: `center/cover`.",
        },
        {
          id: "css-29",
          q: "Что делают `border-radius`, `box-shadow` и `text-shadow`?",
          a: "**border-radius**: скругляет углы, можно задавать для каждого угла отдельно: `border-radius: 10px 5px 10px 5px;` или эллиптически: `50% / 30%;`. **box-shadow**: добавляет тень: `box-shadow: 2px 2px 5px rgba(0,0,0,0.3);` (смещение X, Y, размытие, цвет). Можно несколько теней через запятую. **text-shadow**: тень для текста: `text-shadow: 1px 1px 2px black;`. Поддерживает inset для внутренней тени (только box-shadow).",
        },
      ],
    },
    {
      id: "animation",
      title: "Анимации и трансформации",
      questions: [
        {
          id: "css-14",
          q: "Что такое @keyframes и как сделать анимацию?",
          a: "Пример: `@keyframes slide { 0% { transform: translateX(0); } 100% { transform: translateX(100px); } }` Затем `.element { animation: slide 2s ease-in-out infinite; }`. Анимации могут управляться свойствами: `animation-duration`, `animation-timing-function`, `animation-delay`, `animation-iteration-count`, `animation-direction`, `animation-fill-mode`. В отличие от `transition`, анимации могут иметь несколько ключевых кадров и повторяться.",
        },
        {
          id: "css-25",
          q: "Как работает `transition`?",
          a: "Пример: `transition: all 0.3s ease-in-out;` или `transition: background-color 0.5s, transform 0.2s;`. Свойства: **transition-property** (какие свойства анимировать), **transition-duration** (длительность), **transition-timing-function** (функция времени: ease, linear, cubic-bezier), **transition-delay** (задержка). В отличие от @keyframes, transition работает только при изменении значения свойства (например, при :hover).",
        },
        {
          id: "css-26",
          q: "Что делает свойство `transform`?",
          a: "Основные функции: **translate(x, y)** (перемещение), **rotate(deg)** (поворот), **scale(x, y)** (масштабирование), **skew(x, y)** (искажение). Можно комбинировать: `transform: translateX(10px) rotate(45deg);`. Поддерживает 3D: `rotateX()`, `rotateY()`, `translateZ()`. Transform не вызывает reflow (перерасчёт макета), только repaint, что делает его производительным. Часто используется с transition для анимаций.",
        },
        {
          id: "css-18",
          q: "Что такое `will-change`?",
          a: "Пример: `will-change: transform, opacity;`. Используется для улучшения производительности анимаций и трансформаций. Браузер заранее создаёт композитный слой для элемента, что ускоряет рендеринг. Однако **злоупотребление может ухудшить производительность** из-за лишних слоёв и потребления памяти. Применяйте только при реальной необходимости и удаляйте после завершения анимации.",
        },
      ],
    },
    {
      id: "hide",
      title: "Скрытие элементов",
      questions: [
        {
          id: "css-27",
          hot: true,
          q: "В чём разница между `opacity`, `visibility: hidden` и `display: none`?",
          a: "**opacity: 0** — элемент невидим, но занимает место в потоке, реагирует на события (клики, hover), доступен для скринридеров. **visibility: hidden** — элемент невидим, занимает место, не реагирует на события, недоступен для скринридеров. **display: none** — элемент полностью удаляется из потока, не занимает места, не рендерится, недоступен для скринридеров. Для анимаций лучше opacity, для полного скрытия — display: none.",
        },
      ],
    },
    {
      id: "frameworks",
      title: "CSS-фреймворки",
      questions: [
        {
          id: "css-20",
          q: "Что такое CSS-фреймворки (Bootstrap, Tailwind) и их отличия?",
          a: "**Bootstrap** предлагает готовые решения (кнопки, навигацию, модальные окна) с заранее заданным дизайном, быстро прототипировать. **Tailwind** — это utility-first фреймворк, где вы используете классы типа `p-4`, `text-center` для построения стилей, что даёт больше гибкости и меньший вес готового CSS (PurgeCSS удаляет неиспользуемые классы). Выбор зависит от проекта: Bootstrap для скорости, Tailwind для кастомизации.",
        },
      ],
    },
  ],
};