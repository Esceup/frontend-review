export default {
  id: "css",
  title: "CSS",
  accent: "#43d2ff",
  topics: [
    {
      id: "cascade",
      title: "Каскад и наследование",
      questions: [
        {
          id: "css-01",
          hot: true,
          q: "Что такое каскадность и специфичность в CSS?",
          a: "Каскадность (cascading) — это процесс объединения стилей из разных источников и разрешения конфликтов на основе важности (`!important`), происхождения (user agent, user, author) и специфичности.\n\nСпецифичность — это вес селектора:\n\ninline-стили (1000) > ID (100) > классы, псевдоклассы, атрибуты (10) > теги, псевдоэлементы (1).\n\nЧем выше специфичность, тем приоритетнее правило. При равной специфичности побеждает последнее объявленное.\n\n`!important` переопределяет всё, кроме другого `!important` с большей специфичностью. Используйте его крайне редко.",
        },
        {
          id: "css-02",
          q: "Какие есть способы подключения CSS к HTML?",
          a: '**Внешний**: `<link rel="stylesheet" href="style.css">`. Рекомендуется для разделения стилей и структуры. Кэшируется браузером.\n\n**Внутренний**: `<style> ... </style>` в head. Для специфичных стилей конкретной страницы.\n\n**Инлайн**: `style="color:red;"` у тега. Наивысший приоритет (специфичность 1000), но плохо поддерживается и не переиспользуется.\n\nАльтернатива внешнему — `@import url("style.css")` внутри `<style>`, но он медленнее и блокирует параллельную загрузку.',
        },
        {
          id: "css-22",
          q: "Как работает наследование в CSS?",
          a: "Наследование — механизм, при котором дочерние элементы получают значения некоторых свойств от родителей.\n\nНаследуются: типографика (`font-size`, `color`, `line-height`, `text-align`), списки (`list-style`).\n\nНе наследуются: размеры (`width`, `height`), отступы (`margin`, `padding`), границы (`border`), позиционирование.\n\nМожно принудительно наследовать: `margin: inherit;` или сбросить: `all: initial;`.",
        },
      ],
    },
    {
      id: "selectors",
      title: "Селекторы и псевдоклассы",
      questions: [
        {
          id: "css-03",
          q: "Что такое CSS-селекторы и какие основные типы вы знаете?",
          a: 'Селекторы — основа CSS. Примеры:\n\n`div` — все div.\n`.header` — по классу.\n`#main` — по ID.\n`[type="text"]` — по атрибуту.\n`:first-child` — псевдокласс.\n`::after` — псевдоэлемент.\n\nКомбинаторы:\n\n`div p` — потомок.\n`div > p` — прямой потомок.\n`div + p` — сосед.\n`div ~ p` — все следующие соседи.\n\nСпецифичность растёт с количеством селекторов в цепочке.',
        },
        {
          id: "css-08",
          q: "Что такое псевдоклассы и псевдоэлементы?",
          a: "Псевдоклассы добавляются к селектору с одним двоеточием. Например, `a:hover` или `li:first-child`. Они определяют состояние: наведение, фокус, позиция.\n\nПсевдоэлементы — с двумя двоеточиями: `::before`, `::after`, `::first-line`. Они создают виртуальные элементы или стилизуют части элемента.\n\nНапример, `div::before` вставляет контент до содержимого div. Для работы нужно свойство `content`.",
        },
        {
          id: "css-33",
          q: "Что делают псевдоклассы `:is()`, `:where()` и `:has()`?",
          a: "`:is()` — принимает список селекторов, упрощает запись. `:is(h1, h2, h3) { color: red; }` вместо `h1, h2, h3 { ... }`. Специфичность равна самому специфичному селектору в списке.\n\n`:where()` — то же самое, но специфичность всегда 0. Полезно для сброса стилей.\n\n`:has()` — родительский селектор. Выбирает элемент, содержащий определённых потомков. `div:has(> p)` — div с прямым потомком p.\n\n`:has()` поддерживается во всех современных браузерах с 2023 года.",
        },
        {
          id: "css-35",
          q: "Что такое CSS Nesting (вложенность)?",
          a: "Пример: `.parent { color: red; .child { color: blue; &:hover { color: green; } } }`.\n\nСимвол `&` ссылается на родительский селектор.\n\nПоддерживаются псевдоклассы: `.button { &:hover { ... } }`.\n\nМедиа-запросы: `.container { @media (min-width: 768px) { ... } }`.\n\nУпрощает чтение и поддержку кода, уменьшает дублирование. Поддерживается во всех современных браузерах с 2023 года.",
        },
      ],
    },
    {
      id: "boxmodel",
      title: "Блочная модель и display",
      questions: [
        {
          id: "css-21",
          hot: true,
          q: "Что такое CSS Box Model и из чего он состоит?",
          a: "Box Model состоит из четырёх областей:\n\n**content** — содержимое.\n**padding** — внутренний отступ.\n**border** — граница.\n**margin** — внешний отступ.\n\nПо умолчанию (`content-box`) width и height задаются только для content. Padding и border добавляются сверху.\n\nС `box-sizing: border-box` размеры включают padding и border.\n\nMargin может схлопываться (collapsing) у соседних вертикальных элементов.",
        },
        {
          id: "css-04",
          hot: true,
          q: "В чём разница между `display: block`, `inline`, `inline-block`?",
          a: "**block**: элемент занимает всю доступную ширину, начинается с новой строки. Можно задавать width, height, margin, padding.\n\n**inline**: не переносит строку, ширина определяется содержимым. Нельзя задать высоту и ширину. Вертикальные margin не работают.\n\n**inline-block**: не переносит строку, но можно задавать размеры, отступы и рамки. Гибрид обоих подходов.",
        },
        {
          id: "css-11",
          hot: true,
          q: "Как работает `box-sizing`?",
          a: "По умолчанию `box-sizing: content-box` — ширина задаётся только для содержимого. Padding и border добавляются сверху.\n\n`width: 100px` + `padding: 10px` = итоговая ширина 120px.\n\n`box-sizing: border-box` — ширина включает padding и border.\n\n`width: 100px` + `padding: 10px` = итоговая ширина 100px, содержимое сжимается.\n\nЧасто применяют глобально: `* { box-sizing: border-box; }` для упрощения расчётов макета.",
        },
        {
          id: "css-30",
          q: "В чём разница между `outline` и `border`?",
          a: "`border` — часть box model. Влияет на размеры элемента, может быть скруглена через `border-radius`, поддерживает разные стили для каждой стороны.\n\n`outline` — рисуется поверх border, не влияет на размеры и поток. Не может быть скруглена в некоторых браузерах. Поддерживает `outline-offset`.\n\nOutline обычно используется для фокуса (focus ring). Не наследуется и не может быть задана для отдельных сторон.",
        },
        {
          id: "css-23",
          q: "Что делает свойство `overflow`?",
          a: "Значения:\n\n`visible` — по умолчанию, содержимое выходит за границы.\n`hidden` — обрезается, скролл недоступен.\n`scroll` — всегда показывается скролл, даже если не нужен.\n`auto` — скролл появляется только при необходимости.\n\nМожно задавать отдельно: `overflow-x`, `overflow-y`.\n\nЧасто используется для создания кастомных скроллбаров и обрезки текста (`text-overflow: ellipsis`).",
        },
      ],
    },
    {
      id: "position",
      title: "Позиционирование и слои",
      questions: [
        {
          id: "css-07",
          hot: true,
          q: "Как работает позиционирование (relative, absolute, fixed, sticky)?",
          a: "`static` — по умолчанию, элемент в обычном потоке.\n\n`relative`: элемент смещается относительно своего нормального положения, но занимает место в потоке.\n\n`absolute`: полностью удаляется из потока. Позиционируется относительно ближайшего positioned предка (не static).\n\n`fixed`: относительно viewport. Не двигается при скролле.\n\n`sticky`: ведёт себя как relative до достижения порога (top, bottom), затем фиксируется как fixed.",
        },
        {
          id: "css-12",
          hot: true,
          q: "Что такое `z-index` и когда он работает?",
          a: "Элементы с большим `z-index` находятся выше. `z-index` работает только на элементах с `position` (кроме static).\n\nТакже создаётся новый контекст наложения (stacking context) для дочерних элементов.\n\nПо умолчанию элементы в DOM перекрывают друг друга в порядке следования: последний выше.\n\nОтрицательные значения `z-index` позволяют поместить элемент под родительский.",
        },
        {
          id: "css-24",
          q: "Что такое `float` и `clear` и почему они устарели?",
          a: "`float` (left, right) изначально предназначался для обтекания изображений текстом, но стал использоваться для создания колоночных макетов.\n\nТребовал хаков (clearfix) для очистки. `clear` (left, right, both) запрещал обтекание с указанной стороны.\n\nС появлением flexbox и grid float устарел для layouts, но всё ещё используется для обтекания текста вокруг изображений.",
        },
      ],
    },
    {
      id: "flex",
      title: "Flexbox",
      questions: [
        {
          id: "css-05",
          hot: true,
          q: "Что такое flexbox и для чего он используется?",
          a: "Flexbox позволяет легко выравнивать элементы внутри контейнера по основной и поперечной осям.\n\nОсновные свойства контейнера: `display: flex`, `flex-direction` (row, column), `justify-content` (выравнивание по главной оси), `align-items` (по поперечной), `flex-wrap`.\n\nСвойства дочерних элементов: `flex-grow`, `flex-shrink`, `flex-basis`.\n\nИдеально подходит для компонентов интерфейса, навигации, карточек.",
        },
        {
          id: "css-36",
          q: "Что делает свойство `gap` в flexbox и grid?",
          a: "Пример: `display: flex; gap: 20px;` или `display: grid; gap: 10px 20px;` (rows columns).\n\nВ отличие от margin, gap применяется только между элементами, не добавляя отступы по краям контейнера.\n\nМожно задавать отдельно: `row-gap`, `column-gap`.\n\nРаботает только в flex и grid контейнерах, не в обычном потоке. Упрощает создание равномерных интервалов без хаков с отрицательными margin.",
        },
      ],
    },
    {
      id: "grid",
      title: "Grid и центрирование",
      questions: [
        {
          id: "css-06",
          hot: true,
          q: "Что такое CSS Grid и для чего он используется?",
          a: "CSS Grid Layout даёт возможность управлять расположением элементов в двух измерениях одновременно.\n\nОпределяется сетка через `grid-template-columns`, `grid-template-rows`. Размещение элементов через `grid-column`, `grid-row` или `grid-area`.\n\nПоддерживает именованные области, авто-размещение, `minmax()`, `repeat()`.\n\nПозволяет создавать сложные макеты (dashboard, галереи) без дополнительных обёрток.",
        },
        {
          id: "css-13",
          hot: true,
          q: "Как центрировать элемент по вертикали и горизонтали?",
          a: "Современные способы:\n\n**flexbox**: родитель `display: flex; justify-content: center; align-items: center;`.\n\n**grid**: `display: grid; place-items: center;`.\n\n**absolute**: `top: 50%; left: 50%; transform: translate(-50%, -50%);`.\n\nДля строчных элементов: `text-align: center` и `line-height` равный высоте.\n\nСтарые методы (table-cell, margin: 0 auto с фиксированной шириной) устарели.",
        },
      ],
    },
    {
      id: "units",
      title: "Единицы и функции",
      questions: [
        {
          id: "css-15",
          hot: true,
          q: "Разница между `em` и `rem`?",
          a: "`em` — относительная единица, зависящая от размера шрифта родительского элемента. Может вызывать каскадные эффекты: вложенные элементы увеличиваются.\n\n`rem` (root em) — относительно размера шрифта корневого элемента (`html`, обычно 16px по умолчанию).\n\nИспользование rem упрощает масштабирование всего интерфейса, меняя только корневой размер.\n\nРекомендуется использовать rem для глобальных размеров, em — для локальных (например, padding внутри компонента).",
        },
        {
          id: "css-16",
          q: "Что такое `calc()` в CSS?",
          a: "Пример: `width: calc(50% - 10px);`.\n\nПоддерживает `+`, `-`, `*`, `/`. Позволяет смешивать проценты, пиксели, em и другие единицы.\n\nПолезна для адаптивных макетов, где нужно учитывать отступы.\n\nМожно вкладывать: `calc(100% - calc(20px * 2))`.\n\nРаботает в большинстве свойств, принимающих числовые значения: width, height, margin, padding, font-size.",
        },
        {
          id: "css-31",
          q: "Какие относительные единицы измерения есть в CSS?",
          a: "`vw` (viewport width) — 1% от ширины viewport.\n`vh` (viewport height) — 1% от высоты.\n`vmin` — меньшее из vw и vh.\n`vmax` — большее из vw и vh.\n`ch` — ширина символа 0 в текущем шрифте. Полезно для ограничения ширины текста.\n\nТакже есть `%` (относительно родителя), `em` (относительно шрифта родителя), `rem` (относительно корневого шрифта).\n\nЭти единицы помогают создавать адаптивные макеты без медиа-запросов.",
        },
        {
          id: "css-32",
          q: "Что делают функции `min()`, `max()` и `clamp()`?",
          a: "Примеры:\n\n`width: min(50%, 300px);` — возьмёт меньшее из 50% и 300px.\n`height: max(100px, 50vh);` — возьмёт большее.\n`font-size: clamp(16px, 2vw, 24px);` — значение между 16px и 24px, в идеале 2vw.\n\nЭти функции позволяют создавать адаптивные значения без медиа-запросов.\n\nМожно комбинировать с calc(): `width: calc(min(50%, 300px) - 20px);`.",
        },
      ],
    },
    {
      id: "vars",
      title: "Переменные и @-правила",
      questions: [
        {
          id: "css-10",
          q: "Что такое CSS-переменные (custom properties)?",
          a: 'Пример: `:root { --main-color: #3498db; }` затем `color: var(--main-color);`.\n\nПеременные наследуются, могут быть изменены через медиа-запросы и JavaScript: `element.style.setProperty("--main-color", "red")`. Это облегчает создание динамических тем.\n\nМожно задавать fallback: `var(--color, blue)`.\n\nВ отличие от препроцессоров, работают в рантайме.',
        },
        {
          id: "css-34",
          q: "Что такое @-правила: `@media`, `@supports`, `@layer`?",
          a: "`@media` — применяет стили при определённых условиях (ширина экрана, ориентация): `@media (max-width: 768px) { ... }`.\n\n`@supports` — проверяет поддержку свойства: `@supports (display: grid) { ... }` с fallback через `@supports not`.\n\n`@layer` — определяет каскадные слои для управления приоритетом стилей: `@layer base, components, utilities;`.\n\nСтили в более позднем слое имеют больший приоритет, независимо от специфичности.",
        },
      ],
    },
    {
      id: "adaptive",
      title: "Адаптивность",
      questions: [
        {
          id: "css-09",
          hot: true,
          q: "Как сделать адаптивный дизайн?",
          a: "Основные инструменты:\n\n**media queries** — изменение стилей в зависимости от ширины экрана, ориентации.\n\n**относительные единицы** (%, vw, vh, rem, em) вместо фиксированных пикселей.\n\n**flexbox и grid** для гибкого расположения.\n\n`<picture>` и `<source>` для адаптивных изображений.\n\n`clamp()` для плавного масштабирования.\n\nMobile-first подход: сначала стили для мобильных, затем медиа-запросы для больших экранов.",
        },
        {
          id: "css-37",
          q: "Что делает свойство `aspect-ratio`?",
          a: "Пример: `aspect-ratio: 16 / 9;` или `aspect-ratio: 1;` (квадрат).\n\nЕсли задана width, height вычисляется автоматически, и наоборот.\n\nПолезно для видео, изображений, карточек с фиксированными пропорциями.\n\nМожно комбинировать с min-width и max-width.\n\nЗаменяет старый хак с padding-top в процентах (padding-top: 56.25% для 16:9).\n\nПоддерживается во всех современных браузерах с 2021 года.",
        },
        {
          id: "css-38",
          q: "Что делают свойства `scroll-snap` и `scroll-behavior`?",
          a: "`scroll-snap-type` (на контейнере): `x mandatory` или `y proximity` — определяет ось и строгость привязки.\n\n`scroll-snap-align` (на дочерних элементах): `start`, `center`, `end` — точка привязки.\n\nСоздаёт эффект карусели или слайдера без JavaScript.\n\n`scroll-behavior`: `smooth` — плавная прокрутка при клике на якорные ссылки или программном скролле. `auto` — мгновенная прокрутка (по умолчанию).\n\nМожно задавать для html: `html { scroll-behavior: smooth; }`.",
        },
      ],
    },
    {
      id: "decoration",
      title: "Оформление: фоны и эффекты",
      questions: [
        {
          id: "css-17",
          q: "Как работает `object-fit` для изображений?",
          a: "Свойство `object-fit` работает для заменяемых элементов: img, video, iframe.\n\nЗначения:\n\n`fill` — растянуть, может исказить.\n`contain` — вписать без обрезки, могут появиться пустые области.\n`cover` — заполнить контейнер, обрезая края.\n`none` — оригинальный размер.\n`scale-down` — выбирает наименьший из none и contain.\n\nЧасто используется с `object-position` для управления точкой фокуса.",
        },
        {
          id: "css-19",
          q: "Как задать градиент в CSS?",
          a: "Пример: `background: linear-gradient(to right, red, blue);` или `radial-gradient(circle, yellow, green);`.\n\nМожно задавать несколько цветов и направления: 45deg, to bottom right.\n\nТакже есть `conic-gradient()` для конических градиентов.\n\nГрадиенты генерируются CSS без загрузки изображений, что улучшает производительность.\n\nМожно комбинировать с изображениями: `background: linear-gradient(...), url(...);`.",
        },
        {
          id: "css-28",
          q: "Как работает сокращённая запись `background`?",
          a: 'Пример: `background: #f0f0f0 url("image.png") no-repeat center/cover fixed;`.\n\nПорядок: color, image, repeat, attachment, position/size.\n\nМожно указывать не все свойства, остальные получат значения по умолчанию.\n\nПоддерживает несколько фонов через запятую: `background: url(top.png) top, url(bottom.png) bottom;`.\n\n`background-size` можно задавать через слэш после position: `center/cover`.',
        },
        {
          id: "css-29",
          q: "Что делают `border-radius`, `box-shadow` и `text-shadow`?",
          a: "`border-radius`: скругляет углы. Можно задавать для каждого угла отдельно: `border-radius: 10px 5px 10px 5px;` или эллиптически: `50% / 30%;`.\n\n`box-shadow`: добавляет тень. `box-shadow: 2px 2px 5px rgba(0,0,0,0.3);` (смещение X, Y, размытие, цвет). Можно несколько теней через запятую. Поддерживает `inset` для внутренней тени.\n\n`text-shadow`: тень для текста. `text-shadow: 1px 1px 2px black;`.",
        },
      ],
    },
    {
      id: "animation",
      title: "Анимации и трансформации",
      questions: [
        {
          id: "css-14",
          q: "Что такое @keyframes и как сделать анимацию?",
          a: "Пример: `@keyframes slide { 0% { transform: translateX(0); } 100% { transform: translateX(100px); } }`.\n\nЗатем `.element { animation: slide 2s ease-in-out infinite; }`.\n\nАнимации управляются свойствами: `animation-duration`, `animation-timing-function`, `animation-delay`, `animation-iteration-count`, `animation-direction`, `animation-fill-mode`.\n\nВ отличие от `transition`, анимации могут иметь несколько ключевых кадров и повторяться.",
        },
        {
          id: "css-25",
          q: "Как работает `transition`?",
          a: "Пример: `transition: all 0.3s ease-in-out;` или `transition: background-color 0.5s, transform 0.2s;`.\n\nСвойства:\n\n`transition-property` — какие свойства анимировать.\n`transition-duration` — длительность.\n`transition-timing-function` — функция времени: ease, linear, cubic-bezier.\n`transition-delay` — задержка.\n\nВ отличие от @keyframes, transition работает только при изменении значения свойства, например при `:hover`.",
        },
        {
          id: "css-26",
          q: "Что делает свойство `transform`?",
          a: "Основные функции:\n\n`translate(x, y)` — перемещение.\n`rotate(deg)` — поворот.\n`scale(x, y)` — масштабирование.\n`skew(x, y)` — искажение.\n\nМожно комбинировать: `transform: translateX(10px) rotate(45deg);`.\n\nПоддерживает 3D: `rotateX()`, `rotateY()`, `translateZ()`.\n\nTransform не вызывает reflow (перерасчёт макета), только repaint. Это делает его производительным. Часто используется с transition для анимаций.",
        },
        {
          id: "css-18",
          q: "Что такое `will-change`?",
          a: "Пример: `will-change: transform, opacity;`.\n\nИспользуется для улучшения производительности анимаций и трансформаций. Браузер заранее создаёт композитный слой для элемента, что ускоряет рендеринг.\n\nОднако злоупотребление может ухудшить производительность из-за лишних слоёв и потребления памяти.\n\nПрименяйте только при реальной необходимости и удаляйте после завершения анимации.",
        },
      ],
    },
    {
      id: "hide",
      title: "Скрытие элементов",
      questions: [
        {
          id: "css-27",
          hot: true,
          q: "В чём разница между `opacity`, `visibility: hidden` и `display: none`?",
          a: "`opacity: 0` — элемент невидим, но занимает место в потоке. Реагирует на события (клики, hover). Доступен для скринридеров.\n\n`visibility: hidden` — элемент невидим, занимает место. Не реагирует на события. Недоступен для скринридеров.\n\n`display: none` — элемент полностью удаляется из потока. Не занимает места. Не рендерится. Недоступен для скринридеров.\n\nДля анимаций лучше opacity. Для полного скрытия — display: none.",
        },
      ],
    },
    {
      id: "frameworks",
      title: "CSS-фреймворки",
      questions: [
        {
          id: "css-20",
          q: "Что такое CSS-фреймворки (Bootstrap, Tailwind) и их отличия?",
          a: "**Bootstrap** предлагает готовые решения (кнопки, навигацию, модальные окна) с заранее заданным дизайном. Быстро прототипировать.\n\n**Tailwind** — это utility-first фреймворк, где вы используете классы типа `p-4`, `text-center` для построения стилей. Даёт больше гибкости и меньший вес готового CSS. PurgeCSS удаляет неиспользуемые классы.\n\nВыбор зависит от проекта: Bootstrap для скорости, Tailwind для кастомизации.",
        },
      ],
    },
  ],
};
