export default {
  id: "js",
  title: "JavaScript",
  color: "#ffc857",
  topics: [
    {
      id: "types",
      title: "Типы данных и значения",
      questions: [
        {
          id: "js-01", hot: true,
          q: "Как проверить тип переменной в JavaScript?",
          a: 'Оператор `typeof` возвращает строку с типом: "string", "number", "boolean", "undefined", "symbol", "bigint", "function". **Подводный камень**: `typeof null` возвращает `"object"` (это старая баг-фича JS, которую не могут исправить ради совместимости), а `typeof []` тоже возвращает `"object"`. Поэтому для проверки массивов используйте `Array.isArray(arr)`. Для создания универсальной функции проверки типа применяют `Object.prototype.toString.call(val).slice(8, -1)`, что корректно вернёт "Null", "Array", "Date" и т.д.',
        },
        {
          id: "js-02",
          q: "Что такое Infinity в JavaScript?",
          a: "`Infinity` (и `-Infinity`) — это глобальные свойства, которые ведут себя как математическая бесконечность. `1 / 0` даст `Infinity`. Любое число, умноженное на `Infinity`, даст `Infinity` (кроме `0 * Infinity`, которое даст `NaN`). Проверить значение можно через `Number.isFinite(val)` (в отличие от глобальной `isFinite()`, она не приводит типы принудительно). Также существует `Number.POSITIVE_INFINITY`.",
        },
        {
          id: "js-03", hot: true,
          q: "Что такое NaN и когда возникает?",
          a: '`NaN` возникает при операциях, которые не могут вернуть валидное число: `Math.sqrt(-1)`, `123 * "abc"`, `parseInt("hello")`. **Главная особенность**: `NaN` — это единственное значение в JS, которое не равно самому себе (`NaN === NaN` вернёт `false`). Поэтому для проверки используйте `Number.isNaN(val)` (строгая проверка) или глобальную `isNaN(val)` (которая сначала попытается привести значение к числу). Тип `typeof NaN` возвращает `"number"`.',
        },
        {
          id: "js-04", hot: true,
          q: "В чём разница между null и undefined?",
          a: '`undefined` возвращается при обращении к несуществующему свойству объекта, при вызове функции без `return`, и присваивается переменным при объявлении (`let a;`). `null` используется разработчиком явно, чтобы показать, что значение ожидается, но пока его нет (например, `user.middleName = null`). При нестрогом сравнении `null == undefined` вернёт `true`, но при строгом `null === undefined` — `false`. Историческая странность: `typeof null` возвращает `"object"`.',
        },
      ],
    },
    {
      id: "coercion",
      title: "Приведение типов и сравнение",
      questions: [
        {
          id: "js-05", hot: true,
          q: "Чем отличается оператор `==` от `===`?",
          a: 'Оператор `==` (абстрактное равенство) использует сложный алгоритм приведения типов. Например, `"5" == 5` вернёт `true`, `"" == 0` вернёт `true`, `null == undefined` вернёт `true`. Оператор `===` (строгое равенство) сначала проверяет типы: если они разные, сразу возвращает `false`. **Best practice**: всегда используйте `===` и `!==`, чтобы избежать неочевидных багов, связанных с неявным приведением типов (например, `[] == false` вернёт `true`).',
        },
        {
          id: "js-06",
          q: "К какому типу приводит условие в `if` и тернарном операторе?",
          a: 'Движок JS использует абстрактную операцию `ToBoolean()`. **Falsy** (ложные) значения: `false`, `0`, `-0`, `0n` (bigint), `""` (пустая строка), `null`, `undefined`, `NaN`. **Truthy** (истинные) — всё остальное. **Частые ошибки**: пустой массив `[]` и пустой объект `{}` являются **truthy** (в отличие от Python, где `[]` это false). Строка `"0"` или `"false"` тоже truthy, так как они не пустые. Объект `new Boolean(false)` тоже truthy, потому что это объект.',
        },
      ],
    },
    {
      id: "operators",
      title: "Операторы",
      questions: [
        {
          id: "js-07",
          q: "Что делает оператор логического присваивания `||=`?",
          a: 'Оператор `x ||= y` эквивалентен `x || (x = y)`. Если `x` истинно (truthy), присваивания не происходит. Это удобно для задания значений по умолчанию. **Важное отличие от `??=`**: `||=` сработает, если слева `0` или `""` (так как они falsy), а `??=` (nullish coalescing assignment) сработает только при `null` или `undefined`. Если `0` является валидным значением в вашей логике, используйте `??=` вместо `||=`.',
        },
        {
          id: "js-08",
          q: "Что делает оператор логического присваивания `&&=`?",
          a: "Оператор `x &&= y` эквивалентен `x && (x = y)`. Он полезен, когда нужно обновить свойство объекта или переменную, только если она уже существует и имеет осмысленное значение. Например, `user.settings &&= newSettings` обновит настройки, только если объект `settings` вообще был создан. Если `user.settings` равен `undefined`, присваивания не будет, и ошибка не возникнет.",
        },
        {
          id: "js-09",
          q: "Что делает унарный оператор `!` (логическое НЕ)?",
          a: '`!` сначала применяет `ToBoolean()`, затем инвертирует результат. `!5` → `false`, `!0` → `true`, `!"hello"` → `false`. Часто используется приём **двойного отрицания `!!`** для явного и быстрого приведения любого значения к булевому типу: `!!"string"` вернёт `true`. Это короче, чем `Boolean("string")`, и часто встречается в legacy-коде.',
        },
      ],
    },
    {
      id: "loops",
      title: "Циклы и switch",
      questions: [
        {
          id: "js-10",
          q: "Как выйти из цикла при достижении результата?",
          a: "`break` выходит из самого внутреннего цикла, в котором находится. Если у вас вложенные циклы и нужно выйти из обоих, используйте **метки** (labels): `outerLoop: for (...) { for (...) { if (cond) break outerLoop; } }`. Оператор `return` также прервёт цикл, но только если цикл находится внутри функции (он прервёт выполнение самой функции). `throw new Error()` тоже прервёт цикл и всплывёт до ближайшего `try...catch`.",
        },
        {
          id: "js-11",
          q: "Как перейти к следующей итерации в цикле?",
          a: "`continue` полезен, когда нужно отфильтровать данные внутри цикла без создания нового массива. Например: `if (num % 2 !== 0) continue; console.log(num);` выведет только чётные числа. В цикле `for` после `continue` всё равно выполняется шаг инкремента (`i++`), а затем проверка условия. В `while` и `do...while` нужно быть осторожным: если `continue` стоит до изменения счётчика, можно получить бесконечный цикл.",
        },
        {
          id: "js-12",
          q: "Как выйти одновременно из нескольких уровней циклов сразу?",
          a: "Метки в JS позволяют управлять потоком выполнения в сложных конструкциях. Пример: `search: for (let i = 0; i < 3; i++) { for (let j = 0; j < 3; j++) { if (i + j === 4) break search; } }`. Хотя это работает, в современном коде метки используются редко, так как ухудшают читаемость. Лучше вынести вложенные циклы в отдельную функцию и использовать `return` для выхода, или использовать методы массивов (`find`, `some`), которые можно прервать возвратом `true`.",
        },
        {
          id: "js-13", hot: true,
          q: "Что произойдёт, если в `switch` не поставить `break`?",
          a: 'Это особенность `switch` в C-подобных языках. Если `break` отсутствует, интерпретатор просто продолжает выполнять инструкции ниже. Это может быть багом (забыли `break`), но иногда используется намеренно для группировки: `case 1: case 2: case 3: console.log("1, 2 или 3"); break;`. Многие линтеры (ESLint) имеют правило `no-fallthrough`, которое требует либо ставить `break`, либо писать комментарий `// falls through`.',
        },
        {
          id: "js-14",
          q: "Можно ли в `switch/case` использовать выражения?",
          a: 'Пример использования выражений: `let age = 25; switch (true) { case (age < 18): console.log("child"); break; case (age >= 18 && age < 60): console.log("adult"); break; }`. Здесь `switch` проверяет `true`, и выполняется тот `case`, выражение которого вернуло `true`. Это элегантная (хотя и спорная) замена длинным цепочкам `if...else if`. Сравнение всегда строгое (`===`), поэтому `"5"` и `5` в `case` не совпадут.',
        },
        {
          id: "js-15",
          q: "Как сгруппировать несколько `case` для одного действия?",
          a: 'Правильный синтаксис использует механизм fall-through: `case "admin": case "moderator": console.log("Access granted"); break;`. Вариант с запятыми (`case 1, 2:`) синтаксически валиден (оператор "запятая"), но работать будет не так, как вы ожидаете: JS вычислит все выражения и возьмёт только последнее (то есть `case 2:`). Использование массивов или `||` внутри `case` также приведёт к логическим ошибкам из-за того, как работает строгое сравнение с выражением в `switch`.',
        },
      ],
    },
    {
      id: "scope",
      title: "Область видимости и hoisting",
      questions: [
        {
          id: "js-16",
          q: "Как назначить переменной функцию внутри блока, но вызвать её снаружи?",
          a: "Переменные `let` и `const` имеют блочную область видимости. Если написать `if (true) { function myFn() {} }`, то в strict mode `myFn` не будет доступна снаружи. Решение: `let myFn; if (cond) { myFn = () => {...} } else { myFn = () => {...} } myFn();`. Использование `var` внутри блока поднимет переменную на уровень функции (или глобально), но это считается плохой практикой из-за неочевидного поведения.",
        },
        {
          id: "js-17", hot: true,
          q: "Что произойдёт, если вызвать Function Expression до объявления, если она объявлена через `var`?",
          a: "При `var myFn = function() {};` hoisting поднимает только объявление `var myFn;`, но не присваивание. До строки присваивания `myFn` равна `undefined`. Вызов `myFn()` эквивалентен `undefined()`, что выбрасывает `TypeError: myFn is not a function`. Это ключевое отличие от **Function Declaration** (`function myFn() {}`), которая поднимается целиком и может быть вызвана до строки объявления.",
        },
        {
          id: "js-18", hot: true,
          q: "А если Function Expression объявлена через `let` или `const`?",
          a: "В отличие от `var`, переменные `let` и `const` не инициализируются `undefined` при hoisting. Они находятся в **Temporal Dead Zone (TDZ)** от начала блока до строки объявления. Любое обращение к ним в TDZ (чтение, запись, `typeof`) немедленно выбрасывает `ReferenceError`. Это сделано для отлова багов, когда разработчик случайно использует переменную до её инициализации.",
        },
      ],
    },
    {
      id: "arrows",
      title: "Стрелочные функции и this",
      questions: [
        {
          id: "js-19", hot: true,
          q: "Имеют ли стрелочные функции собственный `this`?",
          a: 'Это главная особенность стрелочных функций. `this` внутри них — это `this` внешней функции на момент создания стрелочной. Это идеально для колбэков: `setTimeout(() => console.log(this.name), 100)` сохранит контекст класса. **Подводный камень**: стрелочные функции нельзя использовать как методы объектов (`const obj = { name: "A", say: () => this.name }` вернёт `undefined`, так как `this` укажет на `window`) и как конструкторы.',
        },
        {
          id: "js-20",
          q: "Имеют ли стрелочные функции свойство `prototype`?",
          a: 'Обычные функции имеют свойство `prototype`, которое используется для создания прототипов объектов при вызове через `new`. У стрелочных функций этого свойства нет (`myArrowFn.prototype === undefined`). Если попытаться выполнить `new (() => {})`, JS выбросит `TypeError: ... is not a constructor`. Это делает стрелочные функции более "лёгкими", но непригодными для ООП на прототипах.',
        },
        {
          id: "js-27",
          q: "Можно ли изменить `this` стрелочной функции через bind/call/apply?",
          a: 'Это критически важно для собеседований. Даже если вы напишете `myArrowFn.bind(newContext)()`, `this` внутри стрелочной функции **не изменится**. Он навсегда "заморожен" в момент создания функции и указывает на `this` внешней области. Именно поэтому стрелочные функции идеальны для `setTimeout`, `Promise.then` и обработчиков событий внутри классов React/Vue, где нужно сохранить контекст экземпляра класса.',
        },
      ],
    },
    {
      id: "objects",
      title: "Объекты",
      questions: [
        {
          id: "js-21",
          q: "Как создать объект в JavaScript?",
          a: 'Литерал `{}` (Object Initializer) — самый частый и быстрый способ. `new Object()` делает то же самое, но выглядит многословно. `Object.create(null)` создаёт "чистый" объект без прототипа (в нём нет даже методов вроде `toString` или `hasOwnProperty`), что полезно для создания словарей (hash maps), где ключами могут быть любые строки, включая "toString". `new Map()` лучше подходит для словарей с ключами любого типа и частым добавлением/удалением.',
        },
        {
          id: "js-22", hot: true,
          q: "Как пройтись по свойствам объекта циклом?",
          a: "`for (let key in obj)` перебирает все перечисляемые (enumerable) строковые ключи, **включая те, что находятся в цепочке прототипов**. Чтобы отфильтровать только собственные свойства, используйте `if (obj.hasOwnProperty(key))` или современный `Object.hasOwn(obj, key)`. Обычные объекты **не являются итерируемыми** (у них нет `Symbol.iterator`), поэтому `for...of` с ними не работает. Лучший современный способ: `Object.entries(obj).forEach(([key, val]) => ...)`.",
        },
        {
          id: "js-23",
          q: "Как удалить свойство из объекта?",
          a: '`delete obj.key` физически удаляет ключ из объекта и возвращает `true` (или `false` для non-configurable свойств). **Важно**: `obj.key = undefined` меняет значение, но ключ остаётся в объекте (`"key" in obj` вернёт `true`, `Object.keys()` его покажет). `delete` не работает на переменных, объявленных через `let/const/var` (только на свойствах объектов), и не удаляет встроенные свойства DOM или прототипов (если удалять напрямую). В строгом режиме попытка удалить неконфигурируемое свойство выбросит ошибку.',
        },
        {
          id: "js-24",
          q: "Как проверить существование свойства в объекте?",
          a: 'Проверка `obj.key !== undefined` **ненадёжна**, потому что свойство может существовать, но иметь значение `undefined` (`{ key: undefined }`). Оператор `in` проверяет всю цепочку прототипов (`"toString" in {}` вернёт `true`). Если нужны только собственные свойства, используйте современный `Object.hasOwn(obj, "key")` (защита от объектов, созданных через `Object.create(null)`) или `obj.hasOwnProperty("key")`. `Object.keys()` возвращает только собственные перечисляемые ключи, но создание массива на каждой проверке снижает производительность.',
        },
      ],
    },
    {
      id: "copy",
      title: "Копирование и клонирование",
      questions: [
        {
          id: "js-25", hot: true,
          q: "Как сделать поверхностное копирование объекта?",
          a: "Поверхностное копирование (shallow copy) создаёт новый объект на первом уровне, но вложенные объекты/массивы копируются **по ссылке**. `{ ...obj }` и `Object.assign({}, obj)` копируют только собственные перечисляемые свойства. Они **не копируют** методы, символы (symbols) и не сохраняют цепочку прототипов. `JSON.parse/stringify` и `structuredClone` делают **глубокое** копирование. `Object.create(obj)` вообще не копирует, а создаёт новый объект, прототипом которого становится `obj`.",
        },
        {
          id: "js-26", hot: true,
          q: "Как сделать глубокое копирование объекта?",
          a: "**`structuredClone(obj)`** — современный стандарт (поддерживается везде с 2022 года). Он копирует циклические ссылки, `Date`, `RegExp`, `Map`, `Set`, `ArrayBuffer`. Но он не может клонировать функции, DOM-узлы и `WeakMap`. **`JSON.parse(JSON.stringify(obj))`** — старый хак. Он теряет `undefined`, функции, символы, превращает `Date` в строки, а `Map/Set` в пустые объекты. Для сложных случаев (с функциями) используют библиотеки вроде `lodash.cloneDeep`.",
        },
        {
          id: "js-58",
          q: "Что делает глобальная функция `structuredClone()`?",
          a: 'До её появления глубокое копирование делали через `JSON.parse(JSON.stringify(obj))`, что ломало `Date` (превращая в строки), теряло `undefined`, `Map`, `Set` и падало на циклических ссылках. `structuredClone()` использует внутренний алгоритм браузера (тот же, что и для `postMessage` в Web Workers / IndexedDB). Она корректно обрабатывает циклы, `RegExp`, `Blob`, но не может клонировать функции, `Error` и DOM-узлы (выбросит `DataCloneError`).',
        },
      ],
    },
    {
      id: "proto",
      title: "Прототипы и конструкторы",
      questions: [
        {
          id: "js-28", hot: true,
          q: "Какие функции могут использоваться как конструкторы (с `new`)?",
          a: "При вызове `new MyFunc()` движок создаёт пустой объект, связывает его с `MyFunc.prototype`, привязывает `this` к этому объекту и возвращает его (если функция не вернула другой объект явно). **Стрелочные функции** не имеют `prototype` и собственного `this`, поэтому `new` выбросит ошибку. **Method shorthand** (`const obj = { myMethod() {} }`) также не может быть конструктором, так как движок помечает их внутренним флагом `[[Construct]] = false`.",
        },
        {
          id: "js-29", hot: true,
          q: "Как добавить метод в функцию-конструктор, чтобы он был общим для всех экземпляров?",
          a: "Если написать `this.method = function() {}` внутри конструктора, функция будет создаваться заново в памяти для **каждого** экземпляра, что неоптимально. Добавление в `Constructor.prototype` создаёт метод один раз, и все экземпляры получают к нему доступ через цепочку прототипов (`__proto__`). В современных классах ES6 (`class User { sayHi() {} }`) методы автоматически попадают в `prototype`.",
        },
        {
          id: "js-40", hot: true,
          q: "В чём разница между `__proto__` и `prototype`?",
          a: "Когда вы вызываете `new Func()`, движок берёт объект `Func.prototype` и записывает его во внутреннее скрытое поле `[[Prototype]]` нового объекта. Доступ к этому полю исторически осуществлялся через геттер/сеттер `__proto__`. В современном коде использовать `__proto__` напрямую не рекомендуется (оно медленное и отсутствует у объектов без прототипа `Object.create(null)`). Правильный API: `Object.getPrototypeOf(obj)` и `Object.setPrototypeOf(obj, proto)`.",
        },
      ],
    },
    {
      id: "vars",
      title: "let/const/var и замыкания",
      questions: [
        {
          id: "js-30", hot: true,
          q: "Что такое `let` и `const`?",
          a: '`let` и `const` появились в ES6 для решения проблем `var` (hoisting, отсутствие блочного скоупа, redeclaration). Они ограничены блоком `{}`. **Главный миф о `const`**: он делает значение неизменяемым. Это неправда. `const` защищает только **ссылку** на значение. Вы не можете написать `constObj = newObj`, но можете мутировать свойства: `constObj.name = "new"`. Для полной иммутабельности используйте `Object.freeze()`.',
        },
        {
          id: "js-31", hot: true,
          q: "Что такое замыкание (closure)?",
          a: "Замыкание возникает, когда функция сохраняет ссылку на своё лексическое окружение (Lexical Environment). Это позволяет создавать приватные переменные, фабрики функций и каррирование. Например, счётчик: `function makeCounter() { let count = 0; return () => count++; }`. Переменная `count` не удаляется сборщиком мусора, так как внутренняя стрелочная функция всё ещё имеет к ней доступ.",
        },
        {
          id: "js-32",
          q: "Что такое область видимости переменной `var`?",
          a: "Если объявить `var` внутри блока `if (true) { var x = 5; }`, переменная `x` будет доступна и снаружи этого блока. Это частая причина багов, особенно в циклах с `setTimeout`, где все итерации ссылаются на одну и ту же переменную. Именно поэтому в ES6 ввели `let` и `const`, которые имеют строгую блочную область видимости.",
        },
      ],
    },
    {
      id: "async",
      title: "Асинхронность и Event Loop",
      questions: [
        {
          id: "js-33", hot: true,
          q: "Как работают `setTimeout` и `setInterval`?",
          a: "JavaScript однопоточный, поэтому таймеры не могут гарантировать точное время выполнения. Если основной поток занят тяжёлыми вычислениями, колбэк таймера будет ждать в очереди (macrotask queue), пока стек вызовов не освободится. Для остановки используются `clearTimeout` и `clearInterval`. Минимальная задержка в современных браузерах для вложенных таймеров составляет 4 мс.",
        },
        {
          id: "js-34", hot: true,
          q: "Как создать микрозадачу (microtask) в JavaScript?",
          a: "Микрозадачи (microtasks) имеют приоритет над макрозадачами (setTimeout, события DOM) и рендерингом. После выполнения каждого синхронного скрипта или макрозадачи движок JS **полностью очищает** очередь микрозадач, прежде чем передать управление браузеру для отрисовки. Это критично для согласованности состояния в промисах. Создать: `queueMicrotask()`, колбэки `Promise.then/catch/finally`, `MutationObserver`.",
        },
        {
          id: "js-35", hot: true,
          q: "Что происходит в Event Loop после выполнения текущей макрозадачи?",
          a: "Это фундаментальное правило Event Loop: **Microtask Queue > Rendering > Macrotask Queue**. После макрозадачи движок выполняет ВСЕ накопившиеся микрозадачи до тех пор, пока их очередь не опустеет, и только потом переходит к рендерингу. Если внутри микрозадачи создавать новые микрозадачи (например, рекурсивные промисы), можно полностью заблокировать интерфейс (infinite loop), так как браузер не получит шанса отрисовать кадр или обработать клики.",
        },
      ],
    },
    {
      id: "strings",
      title: "Строки и регулярные выражения",
      questions: [
        {
          id: "js-41",
          q: "Что возвращает метод `str.match(regexp)`, если совпадений не найдено?",
          a: "Это один из самых коварных моментов в JS. Разработчики часто ожидают пустой массив `[]` и пытаются обратиться к `.length` или применить деструктуризацию, что приводит к падению приложения. Безопасный паттерн: `const matches = str.match(/regex/) || [];`. Если нужно найти все совпадения с группами, лучше использовать современный метод `str.matchAll(regexp)`, который всегда возвращает итерируемый объект.",
        },
        {
          id: "js-42",
          q: "Как заменить все вхождения подстроки с помощью регулярного выражения?",
          a: 'По умолчанию `str.replace()` заменяет **только первое** найденное совпадение. Чтобы заменить все, нужно добавить глобальный флаг: `str.replace(/a/g, "b")`. В ES2021 появился метод `replaceAll()`, который делает то же самое, но работает и со строками без регулярок: `str.replaceAll("a", "b")`. Важно: строки в JS иммутабельны, методы возвращают новую строку, не меняя оригинал.',
        },
        {
          id: "js-43",
          q: "Как наиболее эффективно проверить, содержит ли строка подстроку по регулярке?",
          a: "Метод `test()` оптимизирован для простой проверки факта совпадения и не тратит ресурсы на создание массива с результатами (как `match`). Подводный камень: если регулярка создана с флагом `g` (глобальный поиск), `test()` будет запоминать позицию `lastIndex` и при повторных вызовах может вернуть `false`, даже если совпадение есть. Для простых проверок флаг `g` лучше не использовать.",
        },
      ],
    },
    {
      id: "storage",
      title: "Куки и хранение данных",
      questions: [
        {
          id: "js-36", hot: true,
          q: "Как прочитать куки (cookies) в JavaScript?",
          a: "`document.cookie` возвращает все куки, кроме тех, что помечены флагом `HttpOnly` (такие куки отправляются только на сервер и защищены от XSS-атак). При записи в `document.cookie` вы не перезаписываете все куки, а добавляете или обновляете только указанную пару ключ-значение. Для удобной работы лучше использовать библиотеки (например, `js-cookie`) или современный `CookieStore API`.",
        },
        {
          id: "js-37",
          q: "Как правильно записать значение в куку, если оно содержит спецсимволы?",
          a: 'Куки хранятся как одна длинная строка, разделённая точками с запятой. Если значение содержит `;`, `=` или пробелы, браузер неправильно распарсит её. Поэтому запись выглядит так: `document.cookie = "user=" + encodeURIComponent("John Doe; Admin")`. При чтении нужно применять `decodeURIComponent`. (Современный `cookieStore.set()` делает это автоматически).',
        },
        {
          id: "js-38", hot: true,
          q: "Какой API предоставляет `localStorage` для работы с данными?",
          a: '`localStorage` хранит данные в формате ключ-значение, но **всегда в виде строк**. Если вы сохраните объект (`localStorage.user = {name: "John"}`), он превратится в строку `"[object Object]"`. Поэтому для сложных структур нужно использовать `JSON.stringify()` при записи и `JSON.parse()` при чтении. Прямое присваивание свойств технически работает, но не рекомендуется, так как может конфликтовать со встроенными методами (например, ключ `"length"` или `"setItem"`).',
        },
        {
          id: "js-39",
          q: "Какой максимальный объём данных можно хранить в `localStorage`?",
          a: "В Chrome и Firefox лимит составляет около 5–10 МБ (в Safari может быть меньше). При попытке записать данные сверх лимита браузер синхронно выбросит исключение `DOMException: QuotaExceededError`. Для хранения больших объёмов данных (файлы, базы) нужно использовать **IndexedDB**. Важно помнить, что `sessionStorage` имеет такие же лимиты, но очищается при закрытии вкладки.",
        },
      ],
    },
    {
      id: "dom-events",
      title: "События и формы",
      questions: [
        {
          id: "js-44",
          q: "Какие события происходят при закрытии или обновлении страницы?",
          a: "1) **beforeunload**: позволяет отменить уход пользователя, вернув строку (браузер покажет свой диалог). 2) **pagehide**: современный аналог unload, работает корректно на мобильных устройствах (в iOS Safari `unload` часто не срабатывает при сворачивании). 3) **unload**: последняя возможность выполнить код. Обычные `fetch` или `XMLHttpRequest` в `unload` могут быть прерваны браузером, поэтому для гарантированной отправки аналитики используют `navigator.sendBeacon(url, data)`.",
        },
        {
          id: "js-49",
          q: "На какие элементы можно повесить события `focus` и `blur`?",
          a: 'По умолчанию браузер разрешает фокусироваться только на элементах, с которыми можно взаимодействовать. Если вам нужно сделать кастомный виджет (например, `div`-кнопку) доступным с клавиатуры, вы должны добавить `tabindex="0"`. Это включит элемент в поток табуляции и позволит ему генерировать события `focus` и `blur`. Значение `tabindex="-1"` разрешает программный фокус (через JS), но убирает элемент из табуляции.',
        },
        {
          id: "js-50",
          q: "Всплывают ли события `focus` и `blur` по DOM-дереву?",
          a: "Это историческая особенность DOM-событий. Если вы повесите обработчик `blur` на `<form>`, вы не узнаете, когда пользователь покинет `<input>` внутри неё, так как событие не всплывёт до формы. Специально для решения этой проблемы были добавлены события `focusin` (аналог focus) и `focusout` (аналог blur), которые полноценно всплывают по дереву и позволяют использовать делегирование событий.",
        },
        {
          id: "js-51", hot: true,
          q: "Какое событие срабатывает при каждом изменении значения поля ввода?",
          a: 'Для современных интерфейсов (автодополнение, мгновенная валидация, счётчики символов) используется `input`. Оно реагирует не только на клавиатуру, но и на вставку из буфера обмена, перетаскивание текста и голосовой ввод. Событие `change` на текстовых полях срабатывает только когда пользователь "зафиксировал" ввод (кликнул в другое место или нажал Enter). Для `<select>`, чекбоксов и радио-кнопок `change` срабатывает сразу при изменении.',
        },
        {
          id: "js-52",
          q: "Как отправить форму программно, чтобы сработали валидация и обработчики `submit`?",
          a: 'Классический `form.submit()` отправляет форму на сервер, **но не генерирует событие `submit`** и не запускает HTML-валидацию (атрибуты `required`, `pattern`). Это часто приводило к багам. Метод `form.requestSubmit()` (появился недавно) работает так, будто пользователь нажал кнопку `<button type="submit">`: он проверяет валидность полей, позволяет отменить отправку через `e.preventDefault()` и корректно отрабатывает все слушатели. В качестве fallback-кнопки можно использовать `form.querySelector("input[type=submit]").click()`.',
        },
      ],
    },
    {
      id: "scripts",
      title: "Скрипты и загрузка страницы",
      questions: [
        {
          id: "js-45", hot: true,
          q: "Какие скрипты НЕ блокируют наступление события `DOMContentLoaded`?",
          a: "Обычные `<script>` блокируют парсинг HTML. **`async`** загружаются в фоне и выполняются сразу по готовности, не дожидаясь DOM и не задерживая `DOMContentLoaded`. **`defer`** тоже загружаются в фоне, но ждут полного парсинга HTML и выполняются прямо перед `DOMContentLoaded` (фактически откладывая его наступление). Динамически созданные скрипты по умолчанию ведут себя как `async`.",
        },
        {
          id: "js-46", hot: true,
          q: "Какой атрибут гарантирует выполнение скриптов в порядке их объявления и только после полного парсинга HTML?",
          a: '`defer` идеален для скриптов, которым нужен весь DOM (например, инициализация слайдеров) и которые зависят друг от друга (сначала библиотека, потом ваш код). В отличие от `async`, порядок выполнения `defer`-скриптов гарантирован. Скрипты с `type="module"` по умолчанию ведут себя как `defer` (тоже выполняются после парсинга и сохраняют порядок).',
        },
        {
          id: "js-47",
          q: "Как `async` скрипты влияют на событие `DOMContentLoaded`?",
          a: "Это важное отличие от `defer`. Если `async` скрипт загрузится очень быстро (например, из кэша), он выполнится до `DOMContentLoaded`. Если будет грузиться долго — событие сработает без него. Это делает `async` идеальным для независимых скриптов: счётчиков аналитики, рекламных баннеров, виджетов, которым не нужен DOM и другие скрипты.",
        },
        {
          id: "js-48",
          q: "Как заставить динамически созданный скрипт вести себя как `defer` (соблюдать порядок)?",
          a: "Спецификация HTML гласит, что любые скрипты, созданные через JS и добавленные в DOM, по умолчанию имеют флаг `async = true`. Это значит, что они выполнятся сразу после загрузки, игнорируя порядок добавления. Если вам нужно загрузить библиотеку, а затем плагин к ней динамически, вы обязаны написать: `script.async = false; document.head.append(script);`. Атрибут `defer` на динамических скриптах **не работает**.",
        },
      ],
    },
    {
      id: "promise",
      title: "Промисы",
      questions: [
        {
          id: "js-53", hot: true,
          q: "Какие статические методы есть у объекта `Promise` для работы с массивами промисов?",
          a: "1) `Promise.all([p1, p2])` — резолвится массивом результатов, но если хоть один упадёт (reject), весь `all` немедленно упадёт. 2) `Promise.race([p1, p2])` — возвращает результат (или ошибку) того промиса, который завершился **первым**. 3) `Promise.allSettled([p1, p2])` — ждёт завершения всех, возвращает массив объектов `{status, value/reason}`. 4) `Promise.any([p1, p2])` — ждёт первого **успешного**, падает только если все откажут (AggregateError).",
        },
        {
          id: "js-54", hot: true,
          q: "Как правильно обработать ошибки, возникающие в любом месте цепочки промисов?",
          a: 'Промисы "пробрасывают" ошибки вниз по цепочке до ближайшего обработчика. Если в `.then()` произойдёт синхронный `throw new Error()`, промис автоматически превратится в rejected и управление перейдёт к `.catch()`. `try...catch` внутри `.then()` не нужен. Важно: `.finally()` не принимает аргументов и не обрабатывает ошибки (если только не выбросит новую), он нужен для очистки ресурсов (скрытия спиннера).',
        },
      ],
    },
    {
      id: "network",
      title: "Fetch и сеть",
      questions: [
        {
          id: "js-55", hot: true,
          q: "Что такое `fetch` и как он обрабатывает HTTP-ошибки (404, 500)?",
          a: "Это самая частая ошибка новичков. С точки зрения `fetch`, получение ответа от сервера (даже с кодом 500 Internal Server Error) — это **успешно выполненная сетевая операция**. Reject происходит только если нет интернета, заблокирован CORS или неверный DNS. Поэтому правильный паттерн: `fetch(url).then(res => { if (!res.ok) throw new Error(res.status); return res.json(); }).catch(...)`. `fetch` поддерживает все HTTP-методы, заголовки и тело запроса.",
        },
        {
          id: "js-56", hot: true,
          q: "Как прервать уже отправленный `fetch`-запрос?",
          a: "Промисы в JS не имеют встроенного метода отмены. Для `fetch` был разработан стандарт `AbortController`. Пример: `const ctrl = new AbortController(); fetch(url, { signal: ctrl.signal }); ... ctrl.abort();`. При отмене `fetch` отклоняется с `DOMException: AbortError`, а браузер физически закрывает сетевое соединение, экономя трафик. Этот же контроллер теперь используется и для отмены слушателей событий (`addEventListener(..., { signal })`).",
        },
        {
          id: "js-57", hot: true,
          q: "Что такое WebSocket и его главное отличие от HTTP?",
          a: 'В классическом HTTP клиент всегда инициирует запрос, а сервер отвечает. Чтобы получать обновления, клиенту приходится постоянно "спрашивать" сервер (polling). WebSocket (протокол `ws://` или `wss://`) начинает с HTTP-рукопожатия (`Upgrade: websocket`), после чего TCP-соединение остаётся открытым, и обе стороны могут слать сообщения в любой момент без накладных расходов HTTP-заголовков. Альтернативы: SSE (Server-Sent Events, только от сервера к клиенту) и Long Polling.',
        },
      ],
    },
    {
      id: "proxy",
      title: "Proxy и Reflect",
      questions: [
        {
          id: "js-59", hot: true,
          q: "Что такое `Proxy` в JavaScript?",
          a: 'Синтаксис: `new Proxy(target, handler)`. В объекте `handler` описываются методы-ловушки: `get` (перехват чтения), `set` (записи), `has` (оператора `in`), `deleteProperty` и др. Proxy лежит в основе реактивности современных фреймворков (например, Vue 3 использует его, чтобы отслеживать, какие свойства читает компонент, и перерендеривать его при их изменении). Также с его помощью можно создавать "защищённые" объекты, валидировать данные при записи или реализовывать умные дефолтные значения.',
        },
        {
          id: "js-60",
          q: "Для чего нужен объект `Reflect` в JavaScript?",
          a: '`Reflect` — это не конструктор, а просто объект с методами, которые дублируют внутренние операции JS. Зачем он нужен? Во-первых, он приводит операции к единому функциональному стилю (`Reflect.has(obj, "key")` вместо `"key" in obj`). Во-вторых, методы вроде `Reflect.set()` возвращают `boolean` (успех/неудача), что удобнее, чем `try/catch` при записи в `Object.freeze()`. В-третьих, внутри ловушек `Proxy` именно методы `Reflect` (а не прямое обращение) гарантируют корректную передачу контекста `this` и прототипов.',
        },
      ],
    },
  ],
};