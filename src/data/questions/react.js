export default {
  id: "react",
  title: "React",
  accent: "#61dafb",
  topics: [
    {
      id: "basics",
      title: "Основы React",
      questions: [
        {
          id: "react-01",
          hot: true,
          q: "Что такое React и его основные принципы?",
          a: "React (от Meta) — это библиотека (не фреймворк), отвечающая только за View (представление). Её основные принципы: **компонентный подход** (UI разбивается на независимые переиспользуемые куски), **Virtual DOM** (легковесная копия DOM в памяти для оптимизации обновлений), **однонаправленный поток данных** (данные текут сверху вниз через props) и **декларативность** (вы описываете, как UI должен выглядеть в зависимости от состояния, а React сам решает, как обновить DOM).",
        },
        {
          id: "react-02",
          hot: true,
          q: "Что такое JSX и как он работает под капотом?",
          a: 'Браузеры не понимают JSX. Сборщики (Babel, SWC) преобразуют `<div className="box">Hi</div>` в `React.createElement("div", {className: "box"}, "Hi")` (или `_jsx("div", ...)` в современном React JSX Transform). Результатом является обычный JavaScript-объект (React Element), описывающий узел дерева. JSX позволяет писать выражения внутри фигурных скобок `{}`, но не поддерживает директивы вроде `if` или `for` (используйте тернарные операторы или `&&`).',
        },
        {
          id: "react-03",
          hot: true,
          q: "В чём фундаментальная разница между функциональными и классовыми компонентами?",
          a: "В классовых компонентах `this` всегда указывает на актуальный экземпляр, поэтому `this.state` или `this.props` в асинхронном колбэке (например, setTimeout) вернут самые свежие данные. Функциональные компоненты — это замыкания. Если вы захватили `count` в `useEffect`, он «заморозится» на момент того рендера, в котором эффект был создан (проблема **stale closure**). Сегодня классовые компоненты считаются устаревшим подходом (legacy), всё новое пишется на функциях и хуках.",
        },
        {
          id: "react-04",
          hot: true,
          q: "Что такое props и можно ли их изменять внутри компонента?",
          a: "Props — это механизм передачи данных от родителя к потомку (однонаправленный поток). React строго следит за их неизменяемостью. Если вам нужно изменить данные, вы должны вызвать колбэк, переданный в props, чтобы родитель обновил свой `state` и передал новые props вниз. Прямая мутация props нарушает предсказуемость React и ломает механизм согласования (reconciliation). Исключение: мутирование вложенных объектов технически возможно (JS позволяет), но это антипаттерн, приводящий к багам.",
        },
      ],
    },
    {
      id: "state-hooks",
      title: "Хуки состояния",
      questions: [
        {
          id: "react-05",
          hot: true,
          q: "Как работает `useState` и почему обновление состояния асинхронное?",
          a: "`const [val, setVal] = useState(0);`. Вызов `setVal(1)` не меняет `val` в текущей области видимости (она константа в рамках этого рендера). React ставит обновление в очередь и делает **батчинг** (группировку), чтобы вызвать только один ре-рендер, даже если вы вызвали `setVal` 5 раз подряд. Для обновления на основе предыдущего состояния используйте функциональную форму: `setVal(prev => prev + 1)`. Для сложных объектов используйте `useReducer`.",
        },
        {
          id: "react-09",
          hot: true,
          q: "Когда стоит выбирать `useReducer` вместо `useState`?",
          a: "`useReducer` — это альтернатива `useState`. Он принимает функцию `reducer(state, action) => newState`. Это делает логику изменений предсказуемой и легко тестируемой (как в Redux). Ещё один мощный паттерн: передача `dispatch` (который стабилен и не меняется между рендерами) в глубокие дочерние компоненты вместо множества колбэков `onUpdate`, что позволяет избежать лишних ре-рендеров и необходимости в `useCallback`.",
        },
        {
          id: "react-10",
          hot: true,
          q: "В чём особенность `useRef` по сравнению с `useState`?",
          a: "`useRef` возвращает объект `{ current: initialValue }`. Он используется для двух целей: 1) Доступ к DOM-узлам (`<input ref={myRef} />`). 2) Хранение любых мутируемых значений (ID таймеров, предыдущие props, флаги `isMounted`), которые должны пережить ре-рендер, но не должны его вызывать. Это «лазейка» из парадигмы иммутабельности React, аналогичная полям экземпляра в классовых компонентах.",
        },
      ],
    },
    {
      id: "effects",
      title: "useEffect и побочные эффекты",
      questions: [
        {
          id: "react-06",
          hot: true,
          q: "Что такое `useEffect` и когда выполняется функция очистки (cleanup)?",
          a: "`useEffect(fn, deps)` запускается **после** того, как React обновил DOM. Это идеальное место для fetch-запросов, `addEventListener` или `setInterval`. Возвращаемая функция (cleanup) нужна для предотвращения утечек памяти: она отменяет предыдущий эффект перед запуском нового (если изменились `deps`) или при `unmount`. **Важно**: в React 18 Strict Mode в dev-режиме эффекты намеренно монтируются, размонтируются и снова монтируются, чтобы выявить ошибки в cleanup-функциях.",
        },
        {
          id: "react-07",
          hot: true,
          q: "Почему хуки нельзя вызывать внутри условий или циклов?",
          a: "У функциональных компонентов нет экземпляра (`this`), где можно было бы хранить состояние. Вместо этого React использует внутреннюю структуру данных (связный список ячеек памяти), привязанную к компоненту. При каждом рендере React проходит по этому списку в том же порядке. Если один хук вызовется в `if`, а другой нет, порядок сдвинется, и `useState` вернёт значение чужого хука. Поэтому хуки вызываются **только на верхнем уровне**.",
        },
        {
          id: "react-27",
          hot: true,
          q: "Чем `useLayoutEffect` отличается от `useEffect`?",
          a: "Обычный `useEffect` запускается асинхронно, после того как браузер уже нарисовал кадр (paint). Если внутри него изменить DOM (например, позиционировать тултип), пользователь увидит «мерцание» (сначала тултип в неверном месте, затем прыжок). `useLayoutEffect` запускается синхронно **сразу после мутаций DOM, но до отрисовки**. Браузер заблокируется, пока ваш код не отработает, и нарисует уже исправленный вариант. Используйте его только для измерений (`getBoundingClientRect`) и синхронных DOM-мутаций, чтобы не тормозить UI.",
        },
        {
          id: "react-40",
          hot: true,
          q: "Что такое проблема «устаревшего замыкания» (stale closure) в React?",
          a: "Так как каждый рендер функционального компонента создаёт новое замыкание, все функции внутри него «видят» только те props и state, которые были в этом конкретном рендере. Пример: `useEffect(() => { const id = setInterval(() => console.log(count), 1000); return () => clearInterval(id); }, []);`. Таймер будет вечно печатать `0` (первоначальное значение), потому что он «замкнул» первый рендер. Решения: 1) Добавить `count` в зависимости. 2) Использовать функциональный сеттер `setCount(c => c + 1)`. 3) Хранить актуальное значение в `useRef`.",
        },
      ],
    },
    {
      id: "optimization",
      title: "Контекст и оптимизация",
      questions: [
        {
          id: "react-08",
          hot: true,
          q: "Как `useContext` влияет на производительность и ре-рендеры?",
          a: "Это главная проблема Context API. Если вы положите в один контекст `{ user, theme }` и измените только `theme`, все компоненты, читающие `user`, тоже перерендерятся, потому что ссылка на объект контекста изменилась. Решения: 1) Разделять контексты (отдельно UserContext, отдельно ThemeContext). 2) Мемоизировать `value` через `useMemo`. 3) Использовать селекторы (как в Zustand/Redux) или сторонние библиотеки (например, `use-context-selector`).",
        },
        {
          id: "react-11",
          hot: true,
          q: "В чём разница между `useMemo` и `useCallback`?",
          a: "`useMemo(() => compute(a), [a])` вернёт кэшированный результат `compute`. `useCallback(() => doSomething(a), [a])` вернёт кэшированную ссылку на функцию. Фактически, `useCallback(fn, deps)` эквивалентен `useMemo(() => fn, deps)`. Они нужны для оптимизации: чтобы не передавать новые ссылки на объекты/функции в мемоизированные дочерние компоненты (`React.memo`), что вызвало бы их ненужный ре-рендер.",
        },
        {
          id: "react-12",
          hot: true,
          q: "Как работает `React.memo` и когда он бесполезен?",
          a: '`React.memo` делает поверхностное сравнение (shallow equality) старых и новых props. Если родитель перерендеривается и передаёт `<Child style={{ color: "red" }} />`, то объект `style` создаётся заново при каждом рендере родителя. Ссылки разные → `React.memo` пропустит оптимизацию и перерендерит Child. Чтобы это исправить, нужно мемоизировать props в родителе через `useMemo` и `useCallback`. Не используйте `React.memo` без профилирования — чаще всего это преждевременная оптимизация.',
        },
      ],
    },
    {
      id: "reconciliation",
      title: "Reconciliation и паттерны",
      questions: [
        {
          id: "react-13",
          hot: true,
          q: "Что такое алгоритм согласования (Reconciliation) в React?",
          a: "Когда state меняется, React создаёт новое дерево элементов. Сравнивать деревья «в лоб» (O(n³)) слишком дорого. React использует эвристики (O(n)): 1) Элементы разных типов (div → span) уничтожаются и строятся заново с нуля. 2) Элементы одного типа обновляются по месту. 3) **Ключи (keys)** помогают React понять, какие элементы в списке были перемещены, добавлены или удалены, сохраняя их внутреннее состояние.",
        },
        {
          id: "react-14",
          hot: true,
          q: "Что означает паттерн «Поднятие состояния» (Lifting State Up)?",
          a: "В React данные текут только сверху вниз. Если два компонента на одном уровне должны реагировать на одно и то же событие (например, два поля ввода температуры в Цельсиях и Фаренгейтах), вы не можете передать state напрямую между ними. Вы создаёте состояние в их общем родителе и передаёте вниз значение и функцию-обновитель. Это сохраняет единый источник правды (single source of truth).",
        },
        {
          id: "react-15",
          hot: true,
          q: "В чём разница между контролируемыми и неконтролируемыми компонентами?",
          a: '**Controlled** (управляемые): React является «единым источником правды». `<input value={val} onChange={e => setVal(e.target.value)} />`. Это позволяет мгновенно валидировать ввод, форматировать текст или блокировать кнопки. **Uncontrolled** (неуправляемые): DOM сам хранит значение. `<input defaultValue="test" ref={myRef} />`. Значение читается только в момент отправки формы (`myRef.current.value`). Uncontrolled проще для интеграции с не-React кодом, но даёт меньше контроля над UI.',
        },
        {
          id: "react-21",
          hot: true,
          q: "Что такое «prop drilling» и как его избежать?",
          a: "Если компоненту на самом дне дерева нужна `user`, вам придётся передавать `<Layout user={user}><Sidebar user={user}><Profile user={user} />` — это засоряет код и вызывает лишние ре-рендеры промежуточных узлов. Решения: 1) **Context API** для глобальных данных (тема, локализация, авторизация). 2) **Композиция компонентов** (передавать компоненты как `children`, чтобы промежуточные узлы не знали о данных). 3) **Стейт-менеджеры** (Redux, Zustand, Jotai) для сложной бизнес-логики.",
        },
        {
          id: "react-22",
          q: "Что такое HOC (Higher-Order Component) и Render Props?",
          a: "До появления хуков (React 16.8) это были основные способы шарить логику. **HOC** (Higher-Order Component) — функция `withAuth(Component)`, возвращающая обёртку, которая проверяет авторизацию и рендерит Component. **Render Props** — паттерн, где компонент принимает функцию в props: `<MouseTracker render={(coords) => <div>{coords.x}</div>} />`. Сегодня это считается legacy-кодом, вызывающим «wrapper hell» (матрёшку из обёрток), и повсеместно заменяется на кастомные хуки (`useAuth()`, `useMouse()`).",
        },
      ],
    },
    {
      id: "events-portals",
      title: "События, порталы, фрагменты",
      questions: [
        {
          id: "react-16",
          hot: true,
          q: "Что такое синтетические события (SyntheticEvent) в React?",
          a: "React не вешает обработчики (`onClick`) на каждый конкретный DOM-узел. Вместо этого он использует **делегирование событий**: один глобальный слушатель вешается на корневой узел приложения (в React 17+ на `root`, раньше на `document`). Когда происходит нативное событие, оно всплывает до корня, где React создаёт `SyntheticEvent` (с тем же интерфейсом, что и нативный) и диспатчит его по своему виртуальному дереву. Это улучшает производительность и гарантирует кроссбраузерность.",
        },
        {
          id: "react-17",
          hot: true,
          q: "Для чего нужны порталы (`createPortal`) в React?",
          a: "Частая проблема: модальное окно или выпадающий список рендерится внутри контейнера с `overflow: hidden` или `z-index: 1`, что обрезает или перекрывает его. `ReactDOM.createPortal(child, document.body)` решает это, физически перенося DOM-узел в `<body>`. **Важно**: хотя DOM-узел перенесён, в дереве React он остаётся на своём месте. Это значит, что контекст (Context API), пропсы и всплытие синтетических событий работают так, будто компонент не перемещался.",
        },
        {
          id: "react-18",
          q: "Зачем нужен `React.Fragment` (или `<>...</>`)?",
          a: "Компонент в React должен возвращать один корневой элемент. Раньше приходилось оборачивать списки в `<div>`, что ломало семантику HTML (например, `<tr>` внутри `<tbody>` не может содержать `<div>`) и CSS-сетки. `<React.Fragment>` (или короткий синтаксис `<>...</>`) решает эту проблему: он существует только на этапе Virtual DOM, а в реальный DOM рендерятся только его дети. Короткий синтаксис `<>` не поддерживает атрибут `key`, если фрагмент используется внутри `.map()`.",
        },
        {
          id: "react-28",
          q: "Для чего нужны `forwardRef` и `useImperativeHandle`?",
          a: "По умолчанию `ref`, переданный в компонент, не передаётся дальше (props `ref` не существует). `forwardRef((props, ref) => <input ref={ref} />)` решает это. `useImperativeHandle` используется для инкапсуляции: вместо того чтобы отдавать родителю сырой DOM-узел, вы можете отдать кастомный API. Например, компонент `FancyInput` может выставить только метод `.focus()`, скрыв доступ к `.value` или `.style`.",
        },
      ],
    },
    {
      id: "error-suspense",
      title: "Error Boundaries и Suspense",
      questions: [
        {
          id: "react-19",
          hot: true,
          q: "Что такое Error Boundaries и какие ошибки они НЕ ловят?",
          a: "На данный момент Error Boundaries можно создать **только через класс** (используя `static getDerivedStateFromError` или `componentDidCatch`). Если в `render()` дочернего компонента произойдёт ошибка, приложение не упадёт полностью (white screen), а покажет запасной UI. **Что они НЕ ловят**: 1) Ошибки внутри `onClick` (используйте обычные `try/catch`). 2) Асинхронный код (`setTimeout`, `Promise`). 3) Ошибки в самом Error Boundary. 4) SSR. Сообщество использует библиотеку `react-error-boundary` для удобной обёртки с хуками.",
        },
        {
          id: "react-20",
          hot: true,
          q: "Как работают `React.lazy` и `Suspense`?",
          a: 'Это встроенный механизм для разделения бандла (code splitting). `const LazyComp = React.lazy(() => import("./HeavyComponent"));` создаёт компонент, который загружается только когда понадобится. Его нужно обернуть в `<Suspense fallback={<Spinner />}>`. Пока JS-файл грузится по сети, пользователь увидит спиннер. Это критически важно для уменьшения размера первоначального бандла (Initial Load). Suspense также используется для data fetching (в Relay, React Query, SWR).',
        },
      ],
    },
    {
      id: "react18",
      title: "React 18+",
      questions: [
        {
          id: "react-23",
          hot: true,
          q: "Что делает `React.StrictMode`?",
          a: "`<StrictMode>` не влияет на production-сборку. В dev-режиме он: 1) Предупреждает об использовании устаревших API (например, `componentWillMount`). 2) Проверяет, что функции рендеринга чистые (вызывая их дважды). 3) **Двойной вызов эффектов**: в React 18 StrictMode намеренно монтирует компонент, вызывает `useEffect`, сразу размонтирует (вызывая cleanup) и снова монтирует. Это помогает разработчикам находить баги, когда эффект не очищает за собой подписки или таймеры, что критично для Concurrent Mode.",
        },
        {
          id: "react-26",
          hot: true,
          q: "В чём разница между `ReactDOM.render` и `createRoot` (React 18)?",
          a: "В React 18 архитектура движка была переписана. `ReactDOM.createRoot(container).render(<App />)` создаёт корень, который умеет прерывать рендеринг (Concurrent Mode) и автоматически батчить обновления стейта (даже в промисах и setTimeout). Старый `ReactDOM.render` переведён в legacy-режим: он не поддерживает новые фичи React 18 (Suspense для данных, useTransition) и выдаёт warning в консоли. Для гидратации SSR используется `hydrateRoot`.",
        },
        {
          id: "react-29",
          q: "Зачем нужен хук `useId` (React 18)?",
          a: 'Для доступности (a11y) `<label htmlFor="id">` должен совпадать с `<input id="id">`. Если генерировать ID через `Math.random()` или счётчик, при Server-Side Rendering (SSR) сервер сгенерирует один ID, а клиент при гидратации — другой. React увидит несоответствие и выдаст ошибку «Hydration failed». `useId()` генерирует детерминированные ID (например, `:r1:`), которые гарантированно совпадают на сервере и клиенте. **Не используйте useId для key в списках**.',
        },
        {
          id: "react-30",
          hot: true,
          q: "Что делают `useTransition` и `useDeferredValue` (React 18)?",
          a: "**useTransition** возвращает `[isPending, startTransition]`. Обернув `setState` в `startTransition`, вы говорите React: «это обновление можно прервать, если пользователь начнёт печатать что-то более срочное». **useDeferredValue** принимает значение и возвращает его «отложенную» версию, которая обновится, когда у браузера появится свободное время. Идеально для полей поиска с мгновенным фильтром списка из 10000 элементов: ввод текста (срочно) не лагает, а список (не срочно) обновляется чуть позже.",
        },
        {
          id: "react-31",
          q: "Для чего нужен `useSyncExternalStore`?",
          a: "В Concurrent Mode React может начать рендерить дерево, прерваться, и начать заново. Если внешнее хранилище (например, глобальная переменная или Redux) изменится в этот момент, разные части одного и того же дерева могут отрендериться с разными данными (это называется **tearing**). `useSyncExternalStore(subscribe, getSnapshot)` гарантирует, что React будет читать данные из внешнего источника синхронно и консистентно для всего кадра. Библиотеки вроде Zustand используют его под капотом.",
        },
        {
          id: "react-32",
          hot: true,
          q: "Что такое автоматический батчинг (Automatic Batching) в React 18?",
          a: "До React 18 батчинг работал только внутри синтетических событий React (например, `onClick`). Если вы вызывали `setState` дважды внутри `setTimeout` или после `await fetch()`, React делал **два** ре-рендера. В React 18 автоматический батчинг работает везде. Это значительно улучшает производительность. Если вам по какой-то причине нужно принудительно вызвать синхронный ре-рендер (например, чтобы измерить DOM сразу после обновления), можно использовать `flushSync(() => setState(...))`.",
        },
        {
          id: "react-33",
          hot: true,
          q: "Что такое Concurrent Mode (Concurrent Features) в React?",
          a: "Исторически рендеринг в React был синхронным и блокирующим: начав рендерить большое дерево, JS-поток «зависал» на десятки миллисекунд, и анимации/ввод лагали. Concurrent Mode делает рендеринг **прерываемым**. React может вычислить часть Virtual DOM, увидеть, что пользователь нажал кнопку (высокий приоритет), отбросить текущий черновик, отрендерить кнопку, и затем вернуться к фоновым вычислениям. Это фундамент для `useTransition`, `useDeferredValue` и Suspense для данных.",
        },
      ],
    },
    {
      id: "ssr-rsc",
      title: "SSR и Server Components",
      questions: [
        {
          id: "react-34",
          hot: true,
          q: "Что такое React Server Components (RSC)?",
          a: 'RSC (введены в Next.js App Router) — это революция. В отличие от SSR (где HTML генерируется на сервере, но JS всё равно грузится на клиент для гидратации), Server Components **никогда не отправляют свой JS на клиент**. Они могут напрямую `await db.query()` или читать файлы. Клиент получает только готовый JSON-формат дерева. Для интерактивности (кнопок, хуков) используются **Client Components** (`"use client"`), которые импортируются внутрь серверных. Это даёт нулевой бандл для статического контента.',
        },
        {
          id: "react-35",
          hot: true,
          q: "Что такое гидратация (Hydration) в контексте SSR?",
          a: "При SSR сервер отдаёт полностью готовый HTML. Пользователь сразу видит контент (хороший FCP), но страница пока «мёртвая» (кнопки не кликаются). Затем загружается JS-бандл, и React вызывает `hydrateRoot()`. Он не перестраивает DOM с нуля (это дорого), а проходит по существующему HTML, связывает его с Virtual DOM и вешает слушатели событий. Если HTML от сервера не совпадёт с тем, что ожидает React на клиенте, произойдёт **Hydration Mismatch** (ошибка, и React может перезаписать DOM).",
        },
        {
          id: "react-37",
          hot: true,
          q: "Что такое Server Actions и хук `useActionState` (React 19)?",
          a: 'Вместо создания API-роута (`/api/submit`) и написания `fetch` на клиенте, вы создаёте функцию с `"use server"` и передаёте её прямо в `<form action={myServerAction}>`. React автоматически сериализует данные формы, сделает POST-запрос и выполнит функцию на сервере. Хук `useActionState` (ранее `useFormState`) позволяет легко получить состояние `pending` (для спиннера на кнопке) и `error` (для отображения ошибок валидации под полями) без написания кучи `useState`.',
        },
      ],
    },
    {
      id: "react19",
      title: "React 19 и новые хуки",
      questions: [
        {
          id: "react-36",
          hot: true,
          q: "Что делает новый хук `use()` (React 19)?",
          a: "`const data = use(promise);` — если промис не разрешён, React приостановит рендер (throw Promise) и покажет `<Suspense>` fallback. В отличие от хуков, `use()` **можно вызывать внутри условий и циклов**! Это огромный шаг к упрощению data fetching. Также `use(Context)` заменяет `useContext`, позволяя читать контекст условно. Если промис реджектится, ошибку можно поймать через ближайший Error Boundary.",
        },
        {
          id: "react-38",
          hot: true,
          q: "Для чего нужен хук `useOptimistic` (React 19)?",
          a: "Классический пример — «лайк» или отправка сообщения в чат. Вы не хотите ждать 500мс ответа от сервера, чтобы закрасить сердечко. `useOptimistic` принимает текущее состояние и функцию обновления. При вызове серверной акции он мгновенно подменяет UI на «оптимистичное» значение. Когда Server Action завершается (успехом или ошибкой), React автоматически заменяет оптимистичное состояние на реальное, пришедшее с сервера. Это делает приложения ощущаемыми как нативные.",
        },
        {
          id: "react-39",
          hot: true,
          q: "Что такое кастомные хуки и зачем они нужны?",
          a: "Кастомные хуки — это главная сила React. Если у вас в 5 компонентах повторяется логика подписки на WebSocket или отслеживания размера окна, вы выносите её в `function useWindowSize() { ... }`. В отличие от HOC и Render Props, хуки не добавляют лишних узлов в дерево компонентов и не создают «матрёшки». Важно: каждый вызов кастомного хука создаёт **изолированное** состояние. `useMyHook()` в компоненте А и компоненте Б не поделят данные.",
        },
      ],
    },
    {
      id: "tools",
      title: "Инструменты и best practices",
      questions: [
        {
          id: "react-24",
          hot: true,
          q: "Почему использование индекса массива в качестве `key` — плохая практика?",
          a: "`key` нужен React для идентификации элемента между рендерами. Если список `[A, B, C]` отсортируется в `[C, A, B]`, а ключами были индексы `0, 1, 2`, React подумает, что элемент `0` (бывший A) превратился в C, и просто обновит текст, сохранив внутреннее состояние (например, фокус input-а или локальный state). Это приводит к жутким багам UI. Индексы можно использовать **только** если: 1) список статичен, 2) элементы не имеют уникальных ID, 3) список никогда не сортируется и не фильтруется.",
        },
        {
          id: "react-25",
          q: "Какие возможности предоставляет React DevTools?",
          a: "Незаменимый инструмент. Вкладка **Components** показывает иерархию, позволяет кликнуть на элемент и увидеть его props, state, контекст и значения всех хуков. Можно даже редактировать их в реальном времени. Вкладка **Profiler** записывает процесс рендеринга: сколько времени занял каждый компонент, почему он перерендерился (изменились props, hooks или родитель) и помогает находить узкие места производительности.",
        },
      ],
    },
  ],
};
